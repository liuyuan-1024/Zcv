我想对当前项目做一次代码清理，目标是删除或重构项目中已经不再需要、没有实际作用、重复、过度设计或影响维护性的代码。
清理时请重点关注以下内容：
1. 未使用代码
   - 未被调用的函数、方法、结构体、枚举、模块、变量、常量等。
   - 已废弃但仍保留的旧实现。
   - 无实际用途的导入、依赖、配置项。
2. 重复代码
   - 多处逻辑相同或高度相似的实现。
   - 可以抽象复用，但当前散落在不同位置的代码。
   - 多套并行实现中已经不再需要的一套。
3. 过度设计代码
   - 为未来可能需求预留但当前没有实际使用场景的代码。
   - 不必要的兼容层、适配层、中间封装。
   - 复杂但收益很低的抽象。
4. 临时代码或调试代码
   - 临时打印、测试用逻辑、占位实现。
   - 注释掉但已经没有恢复价值的代码。
   - 为绕过问题而写的临时分支或特殊处理。
5. 无效注释和过期文档
   - 与当前实现不一致的注释。
   - 描述旧逻辑、旧字段、旧模块的注释。
   - 只解释代码表面行为、但没有业务价值的注释。
清理原则：
- 不为了兼容旧代码而保留无用实现。
- 不保留“双轨代码”或多套并行逻辑。
- 不使用 #[allow(dead_code)]、注释掉代码等方式隐藏问题。
- 删除代码前需要确认它确实没有被使用，不能只凭名称判断。
- 清理后应保证项目可以正常构建、测试通过，核心功能不受影响。
- 如果某段代码看起来无用但无法确认，请先标记原因并说明是否建议删除。
- 不删除assets任何资源。
最终目标是让项目代码保持简洁、清晰、可维护，只保留当前架构下真正需要的实现。