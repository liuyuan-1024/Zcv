# 🧱 纯粹的底层编辑引擎能力清单

> 目标：定义一个可以支撑现代 IDE / 编辑器的**单机底层文本编辑引擎**能力边界。  
> 范围：文本存储、坐标转换、事务变异、历史系统、快照、版本、增量事件、位置映射、区间追踪、视口投影，以及为语法分析、LSP、UI 渲染提供所需的**底层文本协作接口**。  
> 非目标：不直接负责渲染、不直接负责主题样式、不直接负责完整 LSP 协议、不直接生成语法树、不直接生成 diagnostics、不负责项目级索引、不负责跨文件搜索、不考虑实时多人协作。

## 当前关键架构决策

- 编辑引擎的**核心编辑坐标**统一采用 `CharOffset`，即 Unicode scalar offset。
- `TextRange` 表示基于 `CharOffset` 的文本区间，不再表示 byte range。
- `ByteOffset` 仍然保留为强类型，但只用于文件字节、编码边界、外部协议适配和显式坐标转换，不作为核心编辑 API 的主坐标。
- M3.5 的目标是完成 `ByteOffset` 编辑语义到 `CharOffset` 编辑语义的彻底迁移，不保留旧 byte-based 编辑 API。
- M4 的高性能文本存储路线选择社区成熟项目 `ropey`，不自研 Rope / Piece Table / Piece Tree。
- `StringStorage` 继续作为参考模型，用于和 `RopeyStorage` 做 differential testing。

---

## 明确不纳入范围

以下能力不属于本清单讨论范围：

- [ ] 实时多人协作
- [ ] CRDT
- [ ] OT / Operational Transform
- [ ] 远程光标
- [ ] 远程选区
- [ ] 用户在线状态 / presence
- [ ] 多端冲突合并
- [ ] 协同编辑历史合并
- [ ] 网络同步协议
- [ ] 离线协作日志重放
- [ ] 完整 LSP Server / Client 实现
- [ ] 完整语法树解析器实现
- [ ] diagnostics 生成逻辑
- [ ] UI 渲染、主题、字体测量
- [ ] 项目级索引
- [ ] 跨文件搜索与替换
- [ ] 文件监听器与冲突交互 UI

---

## 1. 文本存储与内存拓扑

> 引擎的根本职责是以高性能、低内存占用存储和修改文本。当前路线是不自研高性能文本存储，而是在 M4 接入社区成熟 Rope 实现 `ropey`。

- [x] 支持简单文本存储参考模型
  - [x] `StringStorage` 作为语义正确性的 reference model
- [ ] 支持高性能文本存储结构
  - [ ] `RopeyStorage`，基于社区 crate `ropey`
  - [ ] 保持 `TextStorage` 抽象，不把 `ropey` 类型泄漏到 public API
  - [ ] 暂不自研 Rope / Piece Table / Piece Tree
- [ ] 支持大文件下的局部插入、删除、替换
- [ ] 支持超长单行文本的可接受性能
- [ ] 支持 Rope 节点级 Metrics 聚合，优先复用 `ropey` 已有能力
  - [ ] byte 数
  - [x] char / Unicode scalar 数，作为核心编辑坐标
  - [ ] UTF-16 code unit 数
  - [ ] grapheme cluster 数
  - [ ] line 数
- [ ] 支持局部更新，避免全量重建索引
- [ ] 支持内存预算控制
- [ ] 支持大文件降级策略
  - [ ] 限制 Undo 历史
  - [ ] 延迟高成本索引
  - [ ] 降级高成本外部协作接口
  - [ ] 只读或轻量编辑模式

## 2. 快照与版本系统

> 快照是后台搜索、后台分析、外部系统读取文本而不阻塞前台编辑的基础。M3 已完成基于简单存储的基础 Snapshot；M4 接入 `ropey` 后，快照应升级为更接近 O(1) clone 的 Rope snapshot。

- [x] 支持不可变快照
- [ ] 快照创建成本接近 O(1)
  - [x] M3：基础不可变 Snapshot 已完成
  - [ ] M4：基于 `ropey::Rope` 的低成本 clone Snapshot
- [ ] 快照基于引用计数 / 写时复制
  - [ ] M4 接入 `ropey` 后完成
- [x] 快照可安全跨线程读取
- [x] Snapshot 实现 Send + Sync
- [x] 每次事务提交生成单调递增版本号
- [x] 每个 Snapshot 绑定 BufferVersion
- [x] 每个 Delta 绑定源版本与目标版本
- [x] 外部任务结果必须携带其基于的版本号
- [x] 宿主可基于版本号丢弃过期结果
  - [x] 当前基础能力：`is_version_stale` / `is_snapshot_stale`
  - [ ] 真正后台任务调度由宿主层负责

## 3. 强类型约束与不变量设计

> 编辑引擎中存在大量外形相似但语义不同的数值。核心编辑 API 必须使用强类型，且编辑主坐标统一为 `CharOffset`。`ByteOffset` 只能作为显式转换边界，不再作为编辑事务的主坐标。

- [x] 使用强类型区分不同坐标单位
  - [x] ByteOffset，文件字节 / 编码 / 外部适配坐标
  - [x] CharOffset，核心编辑坐标
  - [x] Utf16Offset，LSP / 外部协议坐标，后续补齐转换
  - [ ] GraphemeOffset，用户感知字符边界，后续补齐
  - [x] Line
  - [x] LogicalColumn
  - [x] DisplayColumn
- [x] 使用强类型区分不同文本位置
  - [x] TextRange，基于 `CharOffset` 的文本区间
  - [ ] LineRange
  - [ ] DisplayRange
  - [ ] FoldedRange
  - [ ] SelectionRange
- [x] 使用强类型区分版本与快照
  - [x] BufferVersion
  - [ ] SnapshotVersion，可选；当前 Snapshot 绑定 BufferVersion
  - [x] TransactionId
  - [ ] HistoryNodeId
- [x] 使用强类型表达编辑语义
  - [x] Edit，基于 CharOffset / TextRange
  - [x] Transaction
  - [x] Delta
  - [x] InverseTransaction，目前是 `buffer/mod.rs` 内部历史实现，后续可独立成 public/internal 类型
  - [x] ChangeSet，基于 CharOffset 映射
  - [ ] PositionMap，后续独立抽象
- [ ] 使用强类型表达光标与区间吸附策略
  - [ ] Anchor
  - [ ] Mark
  - [ ] Affinity
  - [ ] Stickiness
  - [ ] Bias
- [x] 禁止在核心编辑 API 中裸露使用 `usize` 表示领域坐标
  - [x] 允许在局部内部实现中使用 `usize`
  - [x] 公共 API、模块边界、事务边界必须使用强类型
- [ ] 用类型系统表达不变量
  - [ ] 非空 Range
  - [x] 已排序 EditList
  - [x] 非重叠 EditSet
  - [ ] 合法 Grapheme 边界
  - [ ] 已归一化 SelectionSet
  - [ ] 已验证 Transaction
- [x] 坐标转换必须显式
  - [x] 不允许隐式把 ByteOffset 当作 CharOffset
  - [x] 不允许隐式把 Logical Column 当作 Display Column
  - [x] 不允许隐式把 UTF-16 Position 当作 CharOffset / ByteOffset

## 4. 配置与策略系统

> 编辑引擎不应该把 tab width、换行符、坐标编码、大文件阈值、词边界等策略硬编码在实现中，而应该通过明确的配置对象注入。

- [ ] 支持 BufferConfig
- [ ] 支持全局配置
- [ ] 支持按语言 / 文件类型覆盖配置
- [ ] 支持按 Buffer 覆盖配置
- [ ] 支持 TabConfig
  - [ ] tab width
  - [ ] indent width
  - [ ] hard tab / soft tab
- [ ] 支持 LineEndingConfig
  - [ ] LF
  - [ ] CRLF
  - [ ] 保留原文件换行风格
  - [ ] 保存时统一换行风格
- [ ] 支持 EncodingConfig
  - [ ] UTF-8
  - [ ] BOM 策略
  - [ ] 非法字节恢复策略
- [ ] 支持 PositionEncodingConfig
  - [ ] UTF-8
  - [ ] UTF-16
  - [ ] UTF-32，可选
- [ ] 支持 LargeFilePolicy
  - [ ] 大文件阈值
  - [ ] 超长行阈值
  - [ ] 降级策略
  - [ ] 历史内存预算
- [ ] 支持 WordBoundaryPolicy
  - [ ] Unicode word boundary
  - [ ] identifier boundary
  - [ ] subword boundary
  - [ ] symbol boundary
- [ ] 支持 DisplayWidthPolicy
  - [ ] tab 展开策略
  - [ ] CJK 宽度策略
  - [ ] emoji 宽度策略
  - [ ] ambiguous width 策略

---

## 5. Buffer 生命周期与状态管理

> Buffer 不只是文本内容本身，还需要维护与版本、保存点、只读状态、临时状态相关的生命周期信息。

- [ ] 支持 BufferId
- [ ] 支持 Buffer 与文件路径 / URI 绑定，可选
- [ ] 支持临时 Buffer / 未命名 Buffer
- [ ] 支持只读 Buffer
- [ ] 支持 dirty state 判断
- [ ] 支持 save point version
- [ ] 支持 last saved version
- [ ] 支持 last synced external version，可选
- [ ] 支持 buffer close 前暴露未保存修改状态
- [ ] 支持 buffer reload 所需的底层文本替换接口
- [ ] 支持 reload 后重建索引、快照与派生缓存
- [ ] 支持 Buffer 状态枚举
  - [ ] Clean
  - [ ] Dirty
  - [ ] ReadOnly
  - [ ] Loading
  - [ ] Reloading
  - [ ] Conflict
- [ ] 支持 current_version 与 saved_version 比较判断是否 dirty
- [ ] 不负责文件监听器、不负责冲突弹窗、不负责用户交互式冲突合并

---

## 6. 坐标系统与文本数学

> 纯文本引擎本质上是多维坐标转换器。当前核心决策是：编辑语义以 `CharOffset` 为主坐标；`ByteOffset`、`Utf16Offset`、`GraphemeOffset` 都必须通过显式转换进入核心编辑模型。

- [ ] 支持多种基础坐标
  - [x] Char / Unicode Scalar Offset，核心编辑坐标
  - [x] Line / Column 坐标
  - [x] Byte Offset，外部字节坐标类型已存在，但不作为编辑主坐标
  - [x] UTF-16 Code Unit Offset，类型已存在，转换后续补齐
  - [ ] Grapheme Cluster Offset
- [ ] 支持多维坐标互转
  - [x] char offset -> line/column
  - [x] line/column -> char offset
  - [ ] byte offset <-> char offset
  - [ ] UTF-8 byte offset <-> UTF-16 position
  - [ ] char / grapheme 之间互转
- [ ] 支持 Unicode 与 Grapheme 边界安全
  - [x] 编辑 API 不再允许落在非法 UTF-8 byte 中间，因为核心编辑坐标是 CharOffset
  - [ ] 光标移动不能切开 emoji / grapheme cluster
  - [ ] 删除操作不能切开组合字符
  - [ ] 编辑操作不能落在非法 Grapheme 边界
- [ ] 支持 Line Index 与 Metrics Cache
  - [x] 快速获取总行数，当前 StringStorage / LineIndex 阶段
  - [x] 快速获取行起止 char offset
  - [ ] Rope 节点维护 bytes / chars / UTF-16 / graphemes / lines 等统计量，M4 接入 ropey 后实现
  - [ ] 编辑后局部更新索引，避免全量扫描
- [ ] 支持换行符数学
  - [x] 支持 LF 与 CRLF 基础边界
  - [x] CRLF 在 byte 层占 2 字节，但在换行语义中视为一个换行
  - [x] 防止光标落在 `
` 中间的文本语义位置
  - [ ] 支持识别、保留或统一文件换行风格
- [ ] 支持 Tab Width 与视觉列宽数学
  - [x] 支持 tab width 配置类型
  - [x] 支持 hard tab / soft tab 策略类型
  - [ ] 支持逻辑 column -> display column
  - [ ] 支持 display column -> 最近合法逻辑 column
  - [ ] `	` 在逻辑 column 中占 1 位，在 display column 中扩展到下一个 tab stop
- [ ] 支持块选区 / 矩形编辑所需的视觉列计算
  - [ ] 块选区按 display column 对齐
  - [ ] 支持短行 virtual space
  - [ ] 支持 tab 展开区域中的吸附策略
  - [ ] 支持矩形插入、删除、替换作为事务提交
- [x] 明确逻辑坐标、视觉坐标与渲染坐标边界
  - [x] 引擎负责 char / line / logical column / display column
  - [x] 引擎保留 byte 坐标用于文件与协议适配
  - [x] 引擎可选提供基础字符宽度策略
  - [x] 引擎不负责真实 pixel 坐标、字体 shaping、ligature 宽度和字距计算
- [ ] 支持坐标错误处理
  - [x] 越界 char 坐标返回错误
  - [x] 非法 line/column 返回错误
  - [x] 非法 CRLF 文本语义边界返回错误
  - [ ] 非法 Grapheme 边界返回错误
  - [ ] display column 落在 tab 中间时有明确吸附策略
  - [ ] 提供严格模式与宽松吸附模式

## 7. 编辑模型与变异管线

> 引擎只接收编辑指令，变异文本状态，不关心指令来源是键盘、鼠标、命令、格式化器还是外部系统。所有编辑范围以 `CharOffset` / `TextRange` 表达。

- [x] 所有变异统一归一化为 Edit 列表
- [x] Edit 至少包含 range 与 replacement text
- [x] 支持插入
- [x] 支持删除
- [x] 支持替换
- [x] 支持批量编辑
- [x] 支持编辑排序
- [x] 支持重叠编辑检测
- [x] 支持冲突编辑拒绝或合并
- [x] 支持越界编辑检测
- [ ] 支持 grapheme 边界保护
- [ ] 支持 line ending normalization
- [x] 支持编辑前后坐标变换，基于 CharOffset / ChangeSet
- [x] 支持 changed ranges 计算，范围基于 CharOffset / TextRange
- [x] 支持生成面向外部订阅者的精确 Delta
- [x] 支持从事务生成 inverse transaction，目前作为历史系统内部实现

## 8. ChangeSet 与位置映射

> 每次事务不仅改变文本，还应该产生旧文本坐标到新文本坐标的映射关系。Delta 偏向事件广播，ChangeSet / PositionMap 偏向内部数学模型。当前 ChangeSet 使用 `CharOffset` / `TextRange`。

- [x] 每次 Transaction 生成 ChangeSet
- [x] ChangeSet 支持 old char position -> new char position
- [ ] ChangeSet 支持 new char position -> old char position
- [x] ChangeSet 支持 old char range -> new char range
- [ ] ChangeSet 支持 new char range -> old char range
- [ ] ChangeSet 支持 anchor 映射
- [ ] ChangeSet 支持 selection 映射，当前仅有轻量 SelectionSnapshot 恢复
- [ ] ChangeSet 支持外部区间元数据的批量更新
- [ ] 支持映射失败或 range 被删除时的失效策略
- [ ] 支持 PositionMap 作为强类型对象
- [x] 支持将 ChangeSet 用于 Undo / Redo、增量分析协作、外部区间元数据更新的基础能力

## 9. 事务系统

> 事务是 Undo、Redo、宏录制、多光标编辑、批量修改的统一提交单位。当前事务 edits 统一使用 `CharOffset` / `TextRange`。

- [x] 所有文本变异必须通过 Transaction 提交
- [x] Transaction 包含 edits
- [x] Transaction 包含 before selection，当前是轻量 SelectionSnapshot
- [x] Transaction 包含 after selection，当前是轻量 SelectionSnapshot
- [x] Transaction 包含 metadata
- [x] Transaction 支持 source 标记
  - [x] keyboard
  - [x] paste
  - [x] delete
  - [x] mouse
  - [x] command
  - [x] formatter
  - [x] external
  - [x] macro
  - [x] undo
  - [x] redo
- [x] Transaction 支持是否进入历史栈
- [x] Transaction 支持合并策略
- [x] Transaction 支持描述文本
- [x] 支持连续输入合并的基础策略
- [ ] 支持连续删除合并
- [ ] 支持连续粘贴合并策略
- [ ] 支持显式 Undo boundary
- [x] 支持事务提交失败回滚
- [x] 支持事务提交后的 Delta 广播

## 10. 光标、选区与多光标数学

> 光标和选区是编辑命令的主要操作对象。

- [ ] 支持 Cursor
- [ ] 支持 Selection
- [ ] 支持 Anchor / Head 模型
- [ ] 支持 Vec<Selection>
- [ ] 支持多光标插入
- [ ] 支持多光标删除
- [ ] 支持多光标替换
- [ ] 支持多光标粘贴
- [ ] 支持选区排序
- [ ] 支持选区规范化
- [ ] 支持重叠选区合并
- [ ] 支持相邻选区处理策略
- [ ] 支持 selection after edit 计算
- [ ] 支持 selection restore for undo/redo
- [ ] 支持光标 affinity / stickiness
  - [ ] 插入点前吸附
  - [ ] 插入点后吸附
- [ ] 支持 word movement
- [ ] 支持 Word Boundary Policy
  - [ ] 可基于 `unicode-segmentation` 实现 Unicode word boundary
  - [ ] 支持 Unicode word boundary
  - [ ] 支持编程语言 identifier boundary
  - [ ] 支持 snake_case 分段策略
  - [ ] 支持 camelCase / PascalCase 分段策略
  - [ ] 支持标点符号分段策略
  - [ ] 支持 operator / symbol 分段策略
  - [ ] 支持语言特定关键字符，如 `_`、`$`、`::`、`.`、`-`
  - [ ] 区分 word、subword、identifier、symbol 四类移动语义
- [ ] 支持 line movement
- [ ] 支持 grapheme movement
- [ ] 支持 page / viewport movement，可选

---

## 11. Anchor、Mark 与 Tracked Range

> 任何需要跟随文本变化的位置都不应该只是裸 offset。

- [ ] 支持 Anchor
- [ ] 支持 Mark
- [ ] 支持 TrackedRange
- [ ] 支持 Range stickiness
- [ ] 支持 range 在插入前后的扩张策略
- [ ] 支持 range 被删除时的失效策略
- [ ] 支持 range 被部分覆盖时的收缩策略
- [ ] 支持搜索结果 range 跟随文本变化
- [ ] 支持外部诊断 range 跟随文本变化
- [ ] 支持断点 / 书签等外部 range 跟随文本变化
- [ ] 支持 folded range 跟随文本变化
- [ ] 支持外部 metadata 挂载到 range
- [ ] 支持 tracked ranges 批量更新
- [ ] 不负责生成外部 metadata 的业务含义

---

## 12. Undo / Redo 与历史系统

> 编辑历史属于编辑引擎核心能力；Undo Tree 可选，但非常适合高级编辑器。当前 M3 已完成线性 Undo / Redo 与基础 Snapshot。

- [x] 支持 Undo
- [x] 支持 Redo
- [ ] 支持 Undo Tree，可选
- [ ] 支持撤销后产生本地历史分支
- [ ] 支持在本地历史分支之间切换
- [x] 支持 Transaction merge
- [ ] 支持 Smart Debounce
- [x] 支持 selection 状态恢复
  - [x] 当前阶段为 SelectionSnapshot / 光标快照恢复
  - [ ] 完整 Selection / Multi Cursor 恢复放到 M6
- [x] 支持历史节点描述
- [ ] 支持历史节点时间戳
- [ ] 支持历史内存预算
- [ ] 支持历史压缩
- [ ] 支持历史截断
- [x] 支持某些事务不进入历史
- [ ] 支持宏回放作为单个或多个 Undo 步骤

## 13. IME 与组合输入支持

> 对中文、日文、韩文等输入法而言，IME 是底层编辑引擎必须认真处理的能力。

- [ ] 支持 composition start
- [ ] 支持 composition update
- [ ] 支持 composition commit
- [ ] 支持 composition cancel
- [ ] 支持预编辑文本
- [ ] 支持组合态选区
- [ ] 支持组合态不污染 Undo 历史
- [ ] 支持提交后合并为合理 Undo 步骤
- [ ] 支持多光标下的 IME 限制或降级策略
- [ ] 支持组合态期间的本地编辑冲突处理

---

## 14. Delta 事件总线

> 引擎应该向外界广播精确增量，而不是整个文件。

- [ ] 每次事务提交产生 Delta
- [ ] Delta 描述插入、删除、替换
- [ ] Delta 包含旧 range
- [ ] Delta 包含新 range
- [ ] Delta 包含旧版本号
- [ ] Delta 包含新版本号
- [ ] Delta 可用于更新 UI
- [ ] Delta 可用于更新外部分析结果
- [ ] Delta 可用于更新搜索结果
- [ ] Delta 可用于同步外部协议适配层
- [ ] 支持多个本地订阅者
- [ ] 支持订阅者延迟处理
- [ ] 支持事件顺序一致性
- [ ] 支持批量 Delta
- [ ] 不要求 Delta 可用于网络协作同步

---

## 15. 折叠、隐藏区间与投影坐标

> 折叠不是简单隐藏文本，而是逻辑坐标与显示坐标之间的映射问题。

- [ ] 支持定义隐藏区间
- [ ] 支持 fold range
- [ ] 支持嵌套 fold range
- [ ] 支持重叠 fold range 检测
- [ ] 支持逻辑坐标到投影坐标映射
- [ ] 支持投影坐标到逻辑坐标映射
- [ ] 支持折叠状态跟随文本编辑更新
- [ ] 支持折叠区间失效策略
- [ ] 支持 viewport projection
- [ ] 支持虚拟行，可选
- [ ] 支持 inline virtual text anchor，可选
- [ ] 不负责 UI 如何绘制折叠占位符

---

## 16. 视口切片与读取接口

> 引擎不渲染，但必须为虚拟滚动和懒加载渲染提供高效切片。核心切片坐标应优先使用 char range；byte range 只作为文件 / 协议适配能力。

- [ ] 支持按 line range 获取文本
- [ ] 支持按 byte range 获取文本，外部适配能力
- [x] 支持按 char range 获取文本，当前可通过 StringStorage 完成
- [ ] 支持按 viewport 获取可见行
- [ ] 支持折叠后的 viewport projection
- [ ] 支持只读 slice
- [ ] 支持零拷贝或低拷贝读取，M4 接入 ropey 后增强
- [ ] 支持大文件随机读取
- [x] 支持获取某行长度的基础能力
- [x] 支持获取总行数
- [x] 支持获取文档总长度，当前以 char length 为核心
- [ ] 支持局部 line cache

## 17. 当前 Buffer 内搜索与替换

> 搜索与替换可以由宿主层调用，但当前 Buffer 内的高性能搜索、结果区间、Replace All 事务非常贴近编辑引擎。

- [ ] 支持当前 Buffer 内普通字符串搜索
- [ ] 支持当前 Buffer 内正则搜索
- [ ] 支持大小写敏感 / 不敏感
- [ ] 支持 whole word
- [ ] 支持多行搜索
- [ ] 支持范围限定搜索
- [ ] 支持基于 Snapshot 的后台搜索
- [ ] 支持搜索结果版本绑定
- [ ] 支持搜索结果 range tracking
- [ ] 支持当前 Buffer 内 replace
- [ ] 支持当前 Buffer 内 replace all
- [ ] replace all 作为单个原子事务
- [ ] 支持正则灾难性回溯防御或超时策略
- [ ] 不负责跨文件搜索与替换

---

## 18. 外部分析系统协作接口

> 纯编辑引擎不直接实现语法树、语义分析或 LSP，但必须提供这些外部系统所需的底层文本协作接口。

- [ ] 每次事务输出 changed ranges
- [ ] 支持向外部语法分析器提供 edit delta
- [ ] 支持旧文本 range 与新文本 range 映射
- [ ] 支持外部结果绑定 BufferVersion
- [ ] 支持外部 diagnostics 的版本绑定与 range tracking
- [ ] 支持外部 semantic tokens 的版本绑定与 range tracking
- [ ] 支持外部 inlay hint 的 anchor / range tracking
- [ ] 支持外部 code lens 的 anchor / range tracking
- [ ] 支持外部 symbol range tracking
- [ ] 支持基于版本号丢弃过期外部分析结果
- [ ] 支持增量高亮所需的文本切片
- [ ] 支持外部协议所需的 UTF-16 坐标转换
- [ ] 不负责生成语法树
- [ ] 不负责生成 diagnostics
- [ ] 不负责执行 LSP 协议

---

## 19. 文件加载与保存边界

> 如果编辑引擎直接处理文件内容，它只应该负责文本进入和离开 Buffer 时的底层语义，不负责文件监听器、弹窗或交互式冲突处理。

- [ ] 支持 UTF-8 文件读取
- [ ] 支持 BOM 检测
- [ ] 支持 BOM 保留或移除策略
- [ ] 支持非法 UTF-8 检测
- [ ] 支持非法字节恢复策略
- [ ] 支持原始编码信息记录
- [ ] 支持换行风格检测
- [ ] 支持保存时换行风格统一
- [ ] 支持文件末尾 newline 保留
- [ ] 支持 atomic save，可选
- [ ] 支持保存前版本检查
- [ ] 支持 reload_from_text / reload_from_snapshot 底层接口
- [ ] 支持 dirty state 判断
- [ ] 不负责文件监听器
- [ ] 不负责冲突提示 UI
- [ ] 不负责多端文件同步冲突合并

---

## 20. 本地并发模型

> 不考虑实时协作，但现代编辑引擎仍然需要明确本地单写多读边界。

- [ ] 可变 Buffer 单线程 / 单入口写入
- [ ] Snapshot 多线程并发读取
- [ ] 所有写入通过事务线性化
- [ ] 后台任务只读 Snapshot
- [ ] 后台任务结果携带版本号
- [ ] 主线程根据版本号接收或丢弃结果
- [ ] Delta 广播保持顺序
- [ ] 支持异步搜索
- [ ] 支持异步外部分析
- [ ] 避免前台输入被后台任务阻塞
- [ ] 支持后台任务取消
- [ ] 支持后台任务超时
- [ ] 支持后台任务内存预算
- [ ] 支持过期任务自动丢弃
- [ ] 支持长任务分片执行
- [ ] 支持低优先级任务让位于前台输入
- [ ] 不处理远程用户并发写入

---

## 21. 外部元数据区间追踪

> 引擎不负责绘制或生成业务信息，但需要让外部系统可靠地把信息附着到文本范围上，并随文本编辑更新。

- [ ] 支持 MetadataRange
- [ ] 支持多 MetadataLayer
- [ ] 支持 search match layer
- [ ] 支持 external diagnostics layer
- [ ] 支持 syntax highlight layer 作为外部结果承载
- [ ] 支持 breakpoint layer
- [ ] 支持 bookmark layer
- [ ] 支持自定义 metadata
- [ ] 支持 metadata range 随文本编辑自动变换
- [ ] 支持 metadata range 失效策略
- [ ] 支持按 viewport 查询 metadata ranges
- [ ] 不负责 UI decoration 渲染
- [ ] 不负责 diagnostics / highlight / breakpoint 的业务生成逻辑

---

## 22. 宏录制与命令模型

> 如果所有操作都能抽象为 Command，宏录制和回放会自然成立。

- [ ] 支持 Command 枚举
- [ ] 支持 Command 序列化
- [ ] 支持 Command 反序列化
- [ ] 支持宏录制
- [ ] 支持宏回放
- [ ] 支持宏回放失败处理
- [ ] 支持宏中的相对光标移动
- [ ] 支持宏中的文本编辑
- [ ] 支持宏作为单个事务提交
- [ ] 支持宏作为多个事务提交
- [ ] 支持命令来源标记
- [ ] 支持命令可重放性测试

---

## 23. 错误处理与防御式设计

> 底层编辑引擎必须对异常输入保持稳定。

- [ ] 越界 range 检测
- [ ] 非法 UTF-8 检测
- [ ] 非 grapheme 边界检测
- [ ] 非法 line/column 检测
- [ ] 重叠 edit 检测
- [ ] 空事务处理
- [ ] 超大粘贴处理
- [ ] Undo 内存爆炸防御
- [ ] 正则搜索超时防御
- [ ] 版本冲突处理
- [ ] panic 边界控制
- [ ] 明确 Result / Error 类型
- [ ] 错误可诊断
- [ ] 错误可测试

---

## 24. 测试与验证能力

> 编辑引擎的正确性比功能数量更重要。

- [ ] 单元测试
- [ ] 集成测试
- [ ] property-based testing
- [ ] fuzz testing
- [ ] Unicode 边界测试
- [ ] CRLF / LF 测试
- [ ] Emoji / grapheme 测试
- [ ] 坐标互转测试
- [ ] ChangeSet / PositionMap 测试
- [ ] 多光标重叠测试
- [ ] Undo / Redo 测试
- [ ] Undo Tree 分支测试
- [ ] Snapshot 并发测试
- [ ] 大文件性能测试
- [ ] 超长行性能测试
- [ ] 随机编辑序列测试
- [ ] 与简单字符串模型做 differential testing
- [ ] 与 Rope / Piece Table 参考实现对比测试

---

## 25. 性能基准与可观测性

> 编辑引擎的性能不能只靠感觉判断，需要可重复的 benchmark 与运行时观测指标。

- [ ] 支持核心操作 benchmark
  - [ ] 插入
  - [ ] 删除
  - [ ] 替换
  - [ ] 批量编辑
  - [ ] 坐标转换
  - [ ] 快照创建
  - [ ] Undo / Redo
  - [ ] viewport slicing
- [ ] 支持大文件 benchmark
- [ ] 支持超长行 benchmark
- [ ] 支持随机编辑序列 benchmark
- [ ] 支持多光标编辑 benchmark
- [ ] 支持当前 Buffer 内搜索 / 替换 benchmark
- [ ] 支持内存占用统计
- [ ] 支持 Undo 历史内存统计
- [ ] 支持索引重建耗时统计
- [ ] 支持快照数量与快照保留时间统计
- [ ] 支持后台任务耗时统计
- [ ] 支持关键路径 tracing，可选

---

# 推荐实现优先级

> 本优先级面向“独立编辑引擎库”的开发顺序。  
> 目标是先得到一个语义正确、可测试、可嵌入的核心，再逐步替换高性能存储结构，并向 `zom ide` 暴露稳定接口。  
> 原则：先验证编辑语义，再优化底层存储；先完成独立库边界，再接入 IDE 协作接口。

---

## M0：库骨架与领域模型

> 先确定编辑引擎作为独立 crate 的边界，不急着追求高性能。

- [ ] 明确 crate 边界
  - [x] 不依赖 UI
  - [x] 不依赖 LSP
  - [x] 不依赖具体渲染框架
  - [ ] 可被 `zom ide` 作为 library 引用

- [x] 建立核心强类型
  - [x] ByteOffset，外部字节坐标，不作为编辑主坐标
  - [x] CharOffset，核心编辑坐标
  - [x] Utf16Offset
  - [x] Line
  - [x] LogicalColumn
  - [x] DisplayColumn
  - [x] Position 逻辑文本位置
  - [x] TextRange，基于 CharOffset
  - [x] BufferVersion
  - [x] TransactionId

- [x] 建立核心错误类型
  - [x] CoordinateError
  - [x] EditError
  - [x] TransactionError
  - [x] StorageError
  - [x] EngineError

- [x] 建立基础配置模型
  - [x] BufferConfig
  - [x] TabConfig
  - [x] LineEndingConfig
  - [x] PositionEncodingConfig
  - [x] LargeFilePolicy

- [ ] 建立测试基础设施
  - [x] 阶段化集成测试：`tests/m0_domain_model.rs` 起步
  - [ ] 随机编辑测试准备
  - [x] 简单字符串参考模型：`StringStorage`

## M1：最小可编辑 Buffer

> 用最简单的后端先跑通编辑语义。当前核心编辑坐标已经决定转向 CharOffset，因此 M1 的能力应按 char-based API 理解。

- [x] 实现 TextStorage 抽象
- [x] 实现简单 StringStorage 参考后端
- [x] 支持 Buffer 创建
- [x] 支持基础文本读取
- [x] 支持 insert，基于 CharOffset
- [x] 支持 delete，基于 CharOffset / TextRange
- [x] 支持 replace，基于 CharOffset / TextRange
- [x] 支持 TextRange 校验，TextRange 基于 CharOffset
- [x] 支持基础 Line Index
- [x] 支持 CharOffset <-> Line / Column
- [x] 支持 BufferVersion 递增
- [x] 支持 dirty state

## M2：编辑事务、Delta 与 ChangeSet

> 把“直接修改文本”升级为“通过事务修改文本”。事务内部统一使用 CharOffset / TextRange。

- [x] 实现 Edit，基于 CharOffset / TextRange
- [x] 实现 EditList 归一化
- [x] 支持编辑排序
- [x] 支持重叠编辑检测
- [x] 支持批量编辑
- [x] 实现 Transaction
- [x] Transaction 提交后生成 Delta
- [x] Transaction 提交后生成 ChangeSet
- [x] 支持 old char position -> new char position
- [x] 支持 old char range -> new char range
- [x] 支持事务失败回滚（buffer/mod.rs/apply_transaction()）
- [x] 支持 changed ranges，范围基于 CharOffset / TextRange

## M3：Undo / Redo 与基础 Snapshot

> 在 Transaction / ChangeSet 稳定后，先基于简单存储验证历史系统和快照语义，再替换高性能存储。

- [x] 支持 inverse transaction
  - [x] 目前是 `buffer/mod.rs` 内部的历史实现
  - [ ] 后期可按需要独立实现为显式 InverseTransaction 类型
- [x] 支持 Undo
- [x] 支持 Redo
- [x] Undo / Redo 恢复文本
- [x] Undo / Redo 恢复 selection snapshot
  - [ ] 完整 Selection / Multi Cursor 恢复放到 M6
- [x] 支持事务合并策略
- [ ] 支持 Smart Debounce
- [x] 支持不可变 Snapshot
- [x] Snapshot 绑定 BufferVersion
- [x] Snapshot 可跨线程读取，测试中有跨线程用例
- [x] 后台任务结果可通过版本号判断是否过期
  - [x] 已完成基础能力：is_version_stale / is_snapshot_stale
  - [ ] 真正后台任务调度不是 engine 当前职责


## M3.5：核心编辑坐标迁移到 CharOffset

> 在接入 ropey 之前，先彻底完成编辑语义迁移。不要保留旧 byte-based 编辑 API，避免长期存在两套编辑模型。

- [x] TextRange 从 ByteOffset 语义迁移为 CharOffset 语义
- [x] SelectionSnapshot 从 ByteOffset 迁移为 CharOffset
- [x] Buffer 编辑 API 全面改为 CharOffset / TextRange
  - [x] insert(CharOffset, text)
  - [x] delete(TextRange)
  - [x] replace(TextRange, text)
- [x] 删除旧 byte-based 编辑 API，不做兼容层
- [x] LineIndex 改为记录 char line starts
- [x] StringStorage::replace 改为 CharOffset range
- [x] Edit / EditList 改为 CharOffset / TextRange
- [x] Transaction 改为 CharOffset edits
- [x] Delta / ChangeSet / changed_ranges 改为 CharOffset / TextRange
- [x] Undo / Redo inverse edits 改为 CharOffset / TextRange
- [x] tests/m0_domain_model.rs 更新为 CharOffset 语义
- [x] tests/m1_buffer.rs 更新为 CharOffset 编辑契约
- [x] tests/m2_transaction.rs 更新为 CharOffset transaction 契约
- [x] tests/m3_history.rs 更新为 CharOffset history 契约
- [x] examples/gpui_m3_testbed.rs 同步切换光标、marker、selection snapshot 到 CharOffset

## M4：高性能文本存储替换

> 在编辑语义、事务、历史和快照跑通后，用社区成熟 Rope 项目替换简单存储。当前决策：使用 `ropey`，不自研 Rope / Piece Table / Piece Tree。

- [ ] 引入 `ropey` 依赖
- [ ] 实现 `RopeyStorage`
- [ ] 保持 `StringStorage` 作为 reference model
- [ ] `TextStorage` 抽象摆脱全文连续 `&str` 假设
- [ ] 支持 Rope 节点级 Metrics，优先复用 ropey 能力
  - [ ] bytes
  - [ ] chars，核心编辑坐标
  - [ ] UTF-16 code units
  - [ ] graphemes，可延后
  - [ ] lines
- [ ] 支持局部插入，基于 CharOffset
- [ ] 支持局部删除，基于 CharOffset / TextRange
- [ ] 支持局部替换，基于 CharOffset / TextRange
- [ ] 支持局部 Metrics 更新，依赖 ropey
- [ ] Snapshot 升级为基于 ropey clone 的低成本快照
- [ ] 支持超长行基础性能测试
- [ ] 支持大文件基础性能测试
- [ ] 与 StringStorage 做 differential testing
- [ ] 不把 `ropey::Rope` 暴露为 public API

## M5：完整坐标系统

> 在 CharOffset 编辑语义和 ropey 存储稳定后，再补齐 Unicode、外部协议坐标和视觉列数学。

### M5A：IDE 必需坐标

- [ ] 支持 ByteOffset <-> CharOffset
- [ ] 支持 UTF-16 Position
- [ ] 支持 CharOffset <-> UTF-16 Position
- [ ] 支持 UTF-8 byte offset <-> UTF-16 position
- [ ] 支持 Grapheme Boundary
- [ ] 支持 grapheme-safe 光标移动
- [ ] 支持 CRLF 边界
- [ ] 支持文件换行风格识别

### M5B：视觉列数学

- [ ] 支持 Tab Width
- [ ] 支持 Tab Stop
- [ ] 支持 logical column -> display column
- [ ] 支持 display column -> logical column
- [ ] 支持 DisplayWidthPolicy

## M6：光标、选区与多光标

> 文本编辑核心稳定后，再实现用户编辑语义。

- [ ] 支持 Cursor
- [ ] 支持 Selection
- [ ] 支持 Anchor / Head 模型
- [ ] 支持 Selection normalize
- [ ] 支持 Selection merge
- [ ] 支持 selection after edit
- [ ] 支持 Vec<Selection>
- [ ] 支持多光标插入
- [ ] 支持多光标删除
- [ ] 支持多光标替换
- [ ] 支持 Word Boundary Policy
- [ ] 可基于 unicode-segmentation 实现 Unicode word boundary
- [ ] 支持 identifier / subword / symbol 移动策略

---

## M6.5：IME 与组合输入

> IME 依赖 Transaction、Selection、Undo merge 与 Grapheme Boundary，适合在光标/选区系统稳定后实现。

- [ ] 支持 composition start
- [ ] 支持 composition update
- [ ] 支持 composition commit
- [ ] 支持 composition cancel
- [ ] 支持预编辑文本
- [ ] 支持组合态选区
- [ ] 支持组合态不污染 Undo 历史
- [ ] 支持 commit 后合并为合理 Undo 步骤
- [ ] 支持多光标下的 IME 限制或降级策略

---

## M7：Anchor、TrackedRange 与外部元数据承载

> 这是之后接入 `zom ide` 的关键。诊断、高亮、搜索结果、断点、书签都需要它。

- [ ] 支持 Anchor
- [ ] 支持 Mark
- [ ] 支持 TrackedRange
- [ ] 支持 Range stickiness
- [ ] 支持 Range invalidation policy
- [ ] 支持搜索结果 range tracking
- [ ] 支持外部 diagnostics range tracking
- [ ] 支持 breakpoint / bookmark range tracking
- [ ] 支持 MetadataLayer
- [ ] 支持按 viewport 查询 metadata ranges

---

## M8：视口、折叠与当前 Buffer 搜索

> 这些能力仍然属于编辑引擎，但可以放在核心稳定之后。

- [ ] 支持 viewport slicing
- [ ] 支持按 line range 获取文本
- [ ] 支持折叠区间
- [ ] 支持 Fold Mapping
- [ ] 支持逻辑坐标 <-> 投影坐标
- [ ] 支持 folded range 跟随编辑变化
- [ ] 支持当前 Buffer 内普通搜索
- [ ] 支持当前 Buffer 内正则搜索
- [ ] 支持 replace
- [ ] 支持 replace all 作为事务提交

---

## M9：外部系统协作接口

> 引擎不实现 LSP / Tree-sitter / diagnostics，但要给它们提供底层支撑。

- [ ] 提供 changed ranges
- [ ] 提供 edit delta
- [ ] 提供版本化 Snapshot
- [ ] 提供 UTF-16 坐标转换
- [ ] 支持外部 diagnostics 版本绑定
- [ ] 支持 semantic tokens range tracking
- [ ] 支持 inlay hint anchor tracking
- [ ] 支持外部分析结果过期丢弃

---

## M10：高级编辑能力

> 这些可以最后做，不影响第一版 Zom IDE 使用编辑引擎。

- [ ] Undo Tree
- [ ] Macro Recording
- [ ] Virtual Lines
- [ ] Inline Virtual Text Anchor
- [ ] 高级历史压缩
- [ ] 高级大文件降级策略
- [ ] 更完整的 fuzz testing
- [ ] 更完整的 benchmark

---

# 判断标准

一个底层编辑引擎是否成熟，可以用下面的问题快速验证：

- [ ] 在 50MB 单行文本中间插入字符是否仍然可接受？
- [ ] 后台正则搜索是否完全不阻塞前台输入？
- [ ] 外部协议返回的 UTF-16 Position 是否能准确映射到 UTF-8 文本？
- [ ] Emoji、组合字符、复杂外文是否不会被光标切开？
- [ ] 多光标重叠删除是否能得到唯一合法结果？
- [ ] Undo 是否能恢复文本与选区？
- [ ] 撤销后再编辑是否能保留本地历史分支？
- [ ] 外部 diagnostics、高亮、断点是否能通过 range tracking 随编辑稳定移动？
- [ ] CRLF 文件保存后是否不会被意外改成 LF？
- [ ] 超大文件、非法 UTF-8、超大粘贴是否不会拖垮引擎？
- [ ] CharOffset 是否作为核心编辑坐标，ByteOffset / Utf16Offset / DisplayColumn 是否只通过显式转换进入对应边界？
- [ ] tab width 改变后 display column 是否能重新计算？
- [ ] word movement 是否能区分 Unicode word、identifier、subword 和 symbol？
- [ ] 后台搜索 / 外部分析任务过期后是否能取消或丢弃？
- [ ] dirty state 是否能通过 current_version 与 saved_version 可靠判断？
- [ ] 核心操作是否有稳定 benchmark 与内存统计？
- [ ] 文档中是否没有把完整 LSP、语法树、diagnostics、UI 渲染误写成编辑引擎职责？
