# 🧪 Zom Editor Engine 测试策略

> 本文档合并“冰山测试策略”与“核心测试哲学：分离机器契约与人类体感”。
>
> 目标：为 `zom-engine` 建立一套清晰、可演进、低 Flaky、适合阶段化开发的测试组织方式。
>
> 核心原则：
>
> * 大部分测试验证纯编辑引擎逻辑。
> * 少量测试验证 GPUI 与编辑引擎的桥接。
> * 自动化测试与人类体感测试在物理目录上分离。
> * 默认优先使用根目录 `tests/` 作为机器契约测试区。
> * `src/tests/` 仅在需要测试 `pub(crate)` 内部不变量时引入，不作为默认主测试区。

---

## 1. 核心测试哲学：分离“机器契约”与“人类体感”

编辑引擎面对两个不同的“用户”：

```text
CI 机器 / cargo test / 编译器
开发者 / UI 接入者 / Dogfooding 使用者
```

因此测试目录也应该反映这种分工。

```text
tests/      给机器跑的自动化契约测试
examples/   给开发者把玩的交互式实验台
benches/    给性能回归分析用的基准测试
src/tests/  可选的 crate 内部测试区，仅测试 public API 覆盖不到的内部不变量
```

最重要的分离是：

```text
tests/    = 机器契约
examples/ = 人类体感
```

这两个目录不要互相替代，也不要混用职责。

---

## 2. 冰山模型总览

本项目采用“冰山模型”测试策略。

```text
水面之上：20%
GPUI testbed / UI 桥接验证 / 手动交互验证 / 人类体感

水面之下：80%
纯 EditorEngine 测试 / 随机测试 / 差分测试 / 性能测试 / CI 自动化屏障
```

### 水面之下：80% 纯编辑引擎测试

主要位置：

```text
tests/
benches/
src/tests/   # 可选，不是默认主入口
```

目标：

* 验证编辑引擎核心逻辑正确性。
* 验证复杂文本边界处理。
* 验证事务、Undo、Redo、坐标、多光标等核心行为。
* 验证大文件、超长行、随机编辑下的稳定性。
* 尽量避免 UI 测试带来的不稳定性。
* 为后续 `zom ide` 引用 `zom-engine` 提供可靠底座。

特点：

* 不依赖 GPUI。
* 不依赖窗口系统。
* 不依赖真实渲染。
* 运行速度快。
* 适合 CI。
* 不容易 flaky。

### 水面之上：20% GPUI 集成与体感测试

主要位置：

```text
examples/
```

目标：

* 验证 GPUI 事件能否正确进入编辑引擎。
* 验证快捷键是否映射到正确 Command。
* 验证 UI 状态是否能与 Engine 状态同步。
* 验证底层 API 与 UI 层的结合度。
* 给开发者提供可把玩、可观察、可肉眼排查问题的全景实验台。

特点：

* 只验证 UI 桥接层，不重复测试编辑算法。
* 主要服务开发者体感，而不是 CI 契约。
* 可以作为手动或半自动 testbed。
* 不作为核心正确性的主要来源。

---

## 3. 当前推荐目录结构

结合当前阶段化开发方式，推荐结构如下：

```text
zom-engine/
├── src/
│   ├── lib.rs
│   ├── types.rs
│   ├── config.rs
│   ├── errors.rs
│   ├── transaction.rs
│   ├── buffer/
│   │   ├── mod.rs
│   │   └── line_index.rs
│   └── storage/
│       ├── mod.rs
│       └── string_storage.rs
│
├── tests/
│   ├── m0_domain_model.rs
│   ├── m1_buffer.rs
│   └── m2_transaction.rs
│
├── examples/
│   ├── gpui_m1_testbed.rs
│   └── gpui_m2_testbed.rs
│
├── benches/
│   ├── edit.rs
│   ├── coordinates.rs
│   ├── large_file.rs
│   └── random_edits.rs
│
└── src/tests/        # 可选目录，当前阶段不必默认创建
    ├── mod.rs
    ├── helpers.rs
    ├── changeset_internal.rs
    ├── piece_tree_internal.rs
    └── anchor_mapping_internal.rs
```

当前阶段建议：

```text
默认：tests/ + examples/
后期需要时：再引入 src/tests/
```

不要一开始就把 `src/tests/` 作为主测试目录，否则会稀释 `tests/ = 机器契约`、`examples/ = 人类体感` 这条最重要的边界。

---

## 4. `tests/`：机器契约测试区

### 4.1 核心定位

`tests/` 是给机器运行的自动化屏障。

```text
核心定位：
给 cargo test / CI 跑的阶段化契约测试，针对单一阶段、单一功能做切片化验证。
```

它应该回答的问题是：

```text
作为一个外部使用者，我通过 public API 使用 zom-engine 时，行为是否稳定、正确、可依赖？
```

### 4.2 当前阶段命名方式

你当前采用的阶段命名很好：

```text
tests/
├── m0_domain_model.rs
├── m1_buffer.rs
└── m2_transaction.rs
```

这种命名方式的优点：

* 和项目实现阶段直接对应。
* 每个阶段形成一个清晰契约。
* 未来回归时很容易判断是哪个阶段能力被破坏。
* 适合渐进式编辑引擎开发。

### 4.3 `tests/` 应该测试什么？

适合放在 `tests/` 的内容：

* Public API 是否稳定。
* 外部 crate 能否正常创建 Buffer。
* 外部 crate 能否提交 Transaction。
* 外部 crate 能否读取文本或 Snapshot。
* 外部 crate 能否执行 Undo / Redo。
* 外部 crate 能否消费 Delta / ChangeSet。
* 外部 crate 能否进行坐标转换。
* 错误输入是否返回明确的错误枚举。
* 阶段性能力是否满足当前 milestone 的契约。

示例：

```rust
use zom_engine::*;

#[test]
fn insert_text_through_public_api() {
    let mut buffer = Buffer::from("hello");
    buffer.insert(5, " world").unwrap();
    assert_eq!(buffer.text(), "hello world");
}
```

### 4.4 `tests/` 不应该测试什么？

不适合放在 `tests/` 的内容：

* 私有函数。
* `pub(crate)` 内部结构。
* 存储结构内部细节。
* 特定 Piece Tree 节点旋转逻辑。
* 内部 cache 失效细节。
* 只为了测试而暴露的内部 helper。
* UI 渲染细节。
* GPUI 事件系统。

如果某个测试必须依赖 crate 内部实现细节，那它不应该勉强塞进 `tests/`，而应该进入 `src/tests/`。

### 4.5 `tests/` 的成功标准

`tests/` 的成功标准是确定性的：

```text
cargo test 通过 = 当前阶段机器契约成立
cargo test 失败 = 某个 public 行为发生回归
```

这里不关心“手感好不好”，只关心“契约是否被破坏”。

---

## 5. `examples/`：GPUI 演进式实验台

### 5.1 核心定位

`examples/` 是给开发者使用的交互式 testbed。

```text
核心定位：
给开发者用来把玩、Dogfooding、肉眼排查 Bug、验证底层 API 与 UI 结合度的全景实验台。
```

它应该回答的问题是：

```text
作为一个真实 UI 接入者，我把 zom-engine 接到 GPUI 上时，交互是否自然、API 是否顺手、状态同步是否可靠？
```

### 5.2 当前阶段命名方式

你当前目录中已有：

```text
examples/
├── gpui_m1_testbed.rs
└── gpui_m2_testbed.rs
```

这个结构适合继续演进：

```text
examples/
├── gpui_m1_testbed.rs
├── gpui_m2_testbed.rs
├── gpui_m3_testbed.rs
├── gpui_m4_testbed.rs
└── gpui_future_testbed.rs
```

### 5.3 递进式演化原则

`examples/` 和 `tests/` 最大的不同是：

```text
tests/    是切片化、独立化、阶段契约化
examples/ 是递进式、全景化、体验继承式
```

例如：

```text
M1 testbed：验证 Buffer + 基础输入 + 渲染
M2 testbed：继承 M1 能力，底层切换到 Transaction
M3 testbed：继承 M2 能力，加入 Undo / Redo
M4 testbed：继承 M3 能力，加入 Selection / Multi Cursor
```

`m2_testbed` 不应该是一个完全孤立的小 demo，而应该是 `m1_testbed` 的平滑升级版。

### 5.4 `examples/` 应该验证什么？

适合放在 GPUI testbed 中验证：

* GPUI 按键事件是否能进入 Engine。
* 快捷键是否映射到正确 Command。
* `cmd-z` / `ctrl-z` 是否触发 Undo。
* `cmd-shift-z` / `ctrl-y` 是否触发 Redo。
* 普通字符输入是否提交 insert transaction。
* 删除键是否提交 delete transaction。
* 鼠标点击是否能更新光标。
* 鼠标拖拽是否能更新 selection。
* IME composition 事件是否能正确进入 Engine。
* viewport 是否能从 Engine 获取可见文本。
* UI selection 是否能与 Engine selection 同步。
* ChangeSet、Anchor、Selection 等抽象能力是否能可视化。
* API 调用是否符合开发者直觉。

### 5.5 `examples/` 不应该测试什么？

不应该在 `examples/` 中重复测试：

* Piece Tree 是否正确插入。
* StringStorage 是否正确删除。
* Undo 算法是否正确。
* UTF-16 坐标是否正确。
* 多光标重叠删除是否正确。
* Grapheme 删除是否正确。
* Search / Replace 算法是否正确。

这些应该由 `tests/`、`src/tests/` 或 `benches/` 覆盖。

### 5.6 `examples/` 的成功标准

`examples/` 的成功标准不是单纯的 `assert_eq!`，而是：

```text
开发者真实敲击键盘、拖拽鼠标、触发命令时，感觉交互自然，状态同步正确，API 接入顺手。
```

例如：

```text
持续长按退格键，观察光标是否自然回退。
拖拽选择文本，观察 selection 是否稳定。
执行批量替换，观察 UI 是否闪烁。
可视化 Anchor 映射，观察追踪点是否符合预期。
```

---

## 6. `src/tests/`：可选的 crate 内部测试区

### 6.1 核心定位

`src/tests/` 是一个可选目录，不是默认主测试目录。

```text
核心定位：
测试 public API 无法覆盖、但又不应该暴露给外部 crate 的 crate 内部行为与 pub(crate) 不变量。
```

它应该回答的问题是：

```text
某些内部算法或内部数据结构的关键不变量，是否在不污染 public API 的前提下被锁死？
```

### 6.2 为什么不默认使用 `src/tests/`？

当前阶段不建议默认引入 `src/tests/`，原因是：

* 你已经有清晰的 `tests/ = 机器契约`。
* 你已经有清晰的 `examples/ = 人类体感`。
* 过早引入 `src/tests/` 会造成测试入口变多。
* 新人会困惑测试到底应该放在哪里。
* 容易诱导开发者测试过多内部实现细节。
* 容易让内部结构过早固化，降低重构自由度。

因此默认原则是：

```text
能用 public API 测，就放 tests/。
不能用 public API 测，且该内部不变量确实重要，才放 src/tests/。
```

### 6.3 什么时候应该引入 `src/tests/`？

当同时满足以下条件时，才适合放入 `src/tests/`：

```text
1. 这个行为很重要，破坏后会导致引擎出错。
2. 这个行为无法通过 public API 精准验证。
3. 这个行为不应该为了测试而暴露成 public API。
4. 这个行为属于 crate 内部不变量，而不是临时实现细节。
5. 测试它能够提高重构安全性，而不是阻碍重构。
```

典型例子：

* Piece Tree / Rope 内部节点不变量。
* ChangeSet 内部 segment 合并规则。
* PositionMap 内部映射单调性。
* Anchor / TrackedRange stickiness 规则。
* Transaction normalize 后的内部排序与不重叠保证。
* 内部 cache invalidation 的关键一致性。
* Storage trait 的多个实现之间的内部一致性。
* Snapshot 与 buffer revision 的内部对应关系。

### 6.4 什么时候不应该放入 `src/tests/`？

以下情况不应该放入 `src/tests/`：

```text
这个行为可以通过 public API 测到。
这个行为只是某个函数当前的实现方式。
这个行为未来很可能随着重构改变。
这个测试只是为了追求覆盖率。
这个测试需要真实 UI 或窗口系统。
这个测试是性能 benchmark。
这个测试依赖过多私有字段，导致重构困难。
```

对应去向：

```text
能通过 public API 测到        -> tests/
需要人类体感或 GPUI 交互      -> examples/
需要性能数据                 -> benches/
非常局部的私有 helper         -> 可以放在源码文件底部小型 #[cfg(test)] mod tests
重要 pub(crate) 内部不变量    -> src/tests/
```

### 6.5 `src/tests/` 与源码文件底部测试的区别

本项目不推荐在每个源码文件底部堆大量测试：

```rust
// 不推荐作为主要方式
#[cfg(test)]
mod tests {
    // 大量测试全部堆在这里
}
```

原因：

* 源码文件会变得很长。
* 编辑器核心逻辑和测试逻辑混杂。
* 后续测试数量增长后难以维护。
* 不利于按能力域组织测试。

但少量非常局部的私有函数测试可以保留在源码文件底部，例如：

* 极小的纯函数。
* 不值得暴露为 `pub(crate)` 的内部 helper。
* 与模块私有状态高度绑定的边界测试。

区别可以这样理解：

```text
源码文件底部 #[cfg(test)] mod tests
    = 极小、局部、贴身的私有 helper 测试

src/tests/
    = 跨模块、按能力域组织的 crate 内部不变量测试
```

### 6.6 `src/tests/` 的可见性边界

Rust 中根目录 `tests/` 是外部集成测试，它只能访问 public API。

```rust
use zom_engine::*;
```

而 `src/tests/` 是 crate 内部测试模块，可以访问 `pub(crate)` 项。

典型声明方式：

```rust
// src/lib.rs

#[cfg(test)]
mod tests;
```

```rust
// src/tests/mod.rs

mod helpers;
mod changeset_internal;
mod piece_tree_internal;
mod anchor_mapping_internal;
```

因此 `src/tests/` 可以测试：

```rust
crate::transaction::normalize_edits(...)
crate::storage::piece_tree::PieceTree
crate::changeset::ChangeSetBuilder
crate::coords::PositionMap
```

但它不应该为了方便测试而访问一切私有细节。

建议边界是：

```text
优先测试 pub(crate) 行为。
谨慎测试 private 字段。
避免锁死纯实现细节。
```

### 6.7 `src/tests/` 推荐组织方式

当项目发展到需要 `src/tests/` 时，建议这样组织：

```text
src/tests/
├── mod.rs
├── helpers.rs
├── storage_invariants.rs
├── transaction_normalize.rs
├── changeset_mapping.rs
├── anchor_mapping.rs
├── snapshot_revision.rs
├── unicode_boundaries.rs
└── differential_internal.rs
```

不要按源码文件机械镜像：

```text
不推荐：
src/tests/buffer_mod_tests.rs
src/tests/transaction_rs_tests.rs
src/tests/storage_string_storage_tests.rs
```

更推荐按能力域组织：

```text
推荐：
src/tests/transaction_normalize.rs
src/tests/changeset_mapping.rs
src/tests/storage_invariants.rs
src/tests/anchor_mapping.rs
```

### 6.8 `src/tests/` 的命名建议

建议文件名显式带出内部性质：

```text
*_internal.rs
*_invariants.rs
*_mapping.rs
*_normalize.rs
```

例如：

```text
changeset_internal.rs
piece_tree_invariants.rs
transaction_normalize.rs
anchor_mapping.rs
storage_consistency.rs
```

这样可以让维护者一眼看出：

```text
这些不是 public API 契约，而是 crate 内部不变量测试。
```

### 6.9 `src/tests/` 的准入清单

每次想把测试放进 `src/tests/` 前，先问五个问题：

```text
1. 这个测试能否通过 public API 写在 tests/？
   能 -> 放 tests/
   不能 -> 继续判断

2. 这个内部行为是否长期稳定？
   不稳定 -> 不要测试，或通过更高层行为测试
   稳定 -> 继续判断

3. 这个内部行为被破坏时，是否会导致严重 bug？
   不会 -> 不要写
   会 -> 继续判断

4. 为了测试它而暴露 public API 是否不合理？
   合理 -> 考虑设计 public API
   不合理 -> 继续判断

5. 这个测试是否帮助未来重构？
   是 -> 放 src/tests/
   否 -> 不要写，避免锁死实现
```

### 6.10 `src/tests/` 的典型使用场景

#### 场景一：Transaction normalize

Public API 只关心最终编辑结果：

```text
输入一组 edits -> buffer 文本正确
```

但内部可能需要保证：

```text
normalize 后 edits 有序
normalize 后 edits 不重叠
normalize 后相邻 edits 按规则合并
非法重叠 edits 被拒绝
```

如果这些规则是 Transaction 系统的重要内部契约，就可以放入：

```text
src/tests/transaction_normalize.rs
```

#### 场景二：ChangeSet 映射

Public API 可以测试：

```text
apply transaction 后 selection 最终位置正确
```

但内部还需要保证：

```text
old position -> new position 单调
new position -> old position 可逆区间符合规则
删除区间内 anchor 的 invalidation 策略稳定
```

这些可以放入：

```text
src/tests/changeset_mapping.rs
```

#### 场景三：Piece Tree / Rope 不变量

Public API 只关心文本最终一致：

```text
insert/delete/replace 后 text() 正确
```

但内部存储可能需要保证：

```text
树高满足约束
节点权重正确
叶子节点长度不超过阈值
中序遍历文本等于快照文本
```

这些可以放入：

```text
src/tests/piece_tree_invariants.rs
```

#### 场景四：Anchor / TrackedRange stickiness

Public API 可能只暴露最终 selection。

但内部规则可能需要保证：

```text
插入发生在 anchor 左侧时 anchor 右移
插入发生在 sticky-left anchor 同点位时 anchor 不动
range 被完全删除时进入 invalid 状态
range 被部分替换时按规则收缩或扩展
```

这些可以放入：

```text
src/tests/anchor_mapping.rs
```

### 6.11 `src/tests/` 的反模式

避免这些做法：

```text
为了测试一个 private 函数，把它改成 pub(crate)。
为了追求覆盖率，把每个内部 helper 都单独测试。
在 src/tests/ 里复制 public API 测试。
把 GPUI 事件测试放入 src/tests/。
让 src/tests/ 依赖真实文件系统、窗口系统、系统剪贴板。
让 src/tests/ 过度依赖结构体字段名。
```

`src/tests/` 的目标不是“看到更多内部细节”，而是“保护少数真正重要的内部不变量”。

---

## 7. `benches/`：性能基准测试

### 7.1 核心定位

`benches/` 用来回答：

```text
编辑引擎在大文件、超长行、随机编辑、高频坐标转换下是否足够快？
```

建议使用：

```toml
[dev-dependencies]
criterion = "0.5"
```

### 7.2 推荐覆盖

```text
benches/
├── edit.rs
├── coordinates.rs
├── large_file.rs
└── random_edits.rs
```

建议覆盖：

* 单点插入。
* 单点删除。
* 范围替换。
* 批量编辑。
* ByteOffset <-> Line / Column。
* UTF-16 Position 转换。
* Snapshot 创建。
* Undo / Redo。
* Viewport slicing。
* 超长行编辑。
* 大文件随机编辑。
* 多光标编辑。
* 当前 Buffer 内搜索。
* Replace All。

### 7.3 `benches/` 不负责什么？

`benches/` 不应该负责正确性主验证。

正确性应该先由：

```text
tests/
src/tests/
```

保证。

Benchmark 的职责是发现：

```text
性能退化
极端输入下的算法复杂度问题
内存分配异常
大文件场景下的瓶颈
```

---

## 8. 差分测试策略

差分测试是编辑引擎非常重要的测试方式。

核心思路：

```text
高性能存储实现执行一组随机编辑
String 参考模型执行同一组随机编辑
最后比较两者文本是否完全一致
```

推荐模型：

```text
PieceTreeBuffer / RopeBuffer
        vs
StringBuffer
```

### 8.1 应覆盖的随机编辑

* 随机插入 ASCII。
* 随机插入中文。
* 随机插入 emoji。
* 随机插入换行。
* 随机删除单字符。
* 随机删除 range。
* 随机替换 range。
* 随机 CRLF 文本。
* 随机超长行。
* 随机多光标编辑。

### 8.2 差分测试目标

* 文本内容一致。
* 行数一致。
* 坐标转换一致。
* undo 后一致。
* redo 后一致。
* snapshot 内容一致。

### 8.3 差分测试放在哪里？

根据测试层级决定：

```text
通过 public API 比较两个 buffer 实现
    -> tests/differential.rs

需要访问 pub(crate) storage 内部结构
    -> src/tests/differential_internal.rs

需要测性能
    -> benches/random_edits.rs
```

---

## 9. Property-based Testing 策略

建议使用：

```toml
[dev-dependencies]
proptest = "1"
```

适合 property-based testing 的能力：

* 坐标互转。
* EditList 归一化。
* Selection merge。
* Range 映射。
* ChangeSet old -> new -> old。
* Undo / Redo 往返。
* Grapheme 边界。
* CRLF 边界。
* Display Column 转换。

### 示例性质

```text
line_column_to_offset(offset_to_line_column(x)) == x

apply(edit)
undo()
文本应恢复原样

apply(edit)
undo()
redo()
文本应等于第一次 apply 后的文本

所有 normalized edits 应该有序且不重叠

所有 selection normalize 后应有序且不重叠
```

### 放置建议

```text
测试 public 行为       -> tests/
测试 pub(crate) 不变量 -> src/tests/
性能性质验证          -> benches/
```

---

## 10. Fuzz Testing 策略

对于编辑引擎，fuzz testing 非常值得后期加入。

建议目录：

```text
fuzz/
├── random_edit_sequence.rs
├── unicode_boundary.rs
├── changeset_mapping.rs
└── undo_redo_roundtrip.rs
```

可以重点 fuzz：

* 非法 UTF-8 边界。
* Grapheme 边界。
* CRLF 边界。
* 随机 range。
* 随机 transaction。
* 随机多光标。
* 随机 Undo / Redo。
* 随机 ChangeSet 映射。

Fuzz 不应该替代普通测试，而应该作为后期增强层。

---

## 11. 三类测试目录的边界对照表

| 维度        | `tests/`        | `src/tests/`     | `examples/`               | `benches/`      |
| --------- | --------------- | ---------------- | ------------------------- | --------------- |
| 核心定位      | 机器契约            | 内部不变量            | 人类体感 / UI testbed         | 性能基准            |
| 主要受众      | CI、编译器、维护者      | 引擎开发者            | 当前开发者、UI 接入者              | 性能调优者           |
| 测试视角      | 外部 crate 视角     | crate 内部视角       | 真实 UI 使用视角                | 性能视角            |
| API 可见性   | 只能访问 public API | 可访问 `pub(crate)` | 使用 public API + UI glue   | 通常使用 public API |
| 设计形态      | 切片化、阶段化         | 能力域内部不变量         | 递进式、全景化                   | 场景化、指标化         |
| 运行方式      | `cargo test`    | `cargo test`     | `cargo run --example ...` | `cargo bench`   |
| 成功标准      | assert 全过       | 内部不变量成立          | 开发者体感自然                   | 性能无明显退化         |
| 是否进 CI    | 是               | 是                | 默认否                       | 可选              |
| 是否依赖 GPUI | 否               | 否                | 是                         | 否               |
| 是否测试算法正确性 | 是               | 是，限内部不变量         | 否，避免重复                    | 否，主要测性能         |

---

## 12. 测试放置决策树

当你要新增一个测试时，按这个顺序判断：

```text
1. 这是 UI / GPUI / 人类体感 / API 接入手感测试吗？
   是 -> examples/
   否 -> 继续

2. 这是性能测试吗？
   是 -> benches/
   否 -> 继续

3. 这个测试能否只通过 public API 完成？
   是 -> tests/
   否 -> 继续

4. 这个测试是否保护重要的 pub(crate) 内部不变量？
   是 -> src/tests/
   否 -> 不要写，或者改成 public 行为测试
```

更简洁地说：

```text
默认 tests/
UI 进 examples/
性能进 benches/
只有重要内部不变量进 src/tests/
```

---

## 13. 推荐阶段化测试文件划分

### 当前阶段

```text
tests/
├── m0_domain_model.rs
├── m1_buffer.rs
└── m2_transaction.rs

examples/
├── gpui_m1_testbed.rs
└── gpui_m2_testbed.rs
```

### 后续阶段示例

```text
tests/
├── m0_domain_model.rs
├── m1_buffer.rs
├── m2_transaction.rs
├── m3_history.rs
├── m4_selection.rs
├── m5_coordinates.rs
├── m6_unicode.rs
├── m7_search_replace.rs
└── m8_public_api_smoke.rs

examples/
├── gpui_m1_testbed.rs
├── gpui_m2_testbed.rs
├── gpui_m3_testbed.rs
├── gpui_m4_testbed.rs
└── gpui_future_testbed.rs
```

如果后期内部复杂度上来，再加入：

```text
src/tests/
├── mod.rs
├── helpers.rs
├── transaction_normalize.rs
├── changeset_mapping.rs
├── piece_tree_invariants.rs
├── anchor_mapping.rs
└── storage_consistency.rs
```

### `tests/m*.rs` 推荐职责

#### `tests/m0_domain_model.rs`

测试：

* 基础类型构造。
* Range / Position / Span 等领域模型。
* 错误类型。
* 基础边界条件。

#### `tests/m1_buffer.rs`

测试：

* 创建 Buffer。
* 读取文本。
* insert。
* delete。
* replace。
* 空文本编辑。
* 文首编辑。
* 文末编辑。
* 多行编辑。
* 基础错误处理。

#### `tests/m2_transaction.rs`

测试：

* Transaction 提交。
* 批量编辑。
* 事务失败回滚。
* Delta 生成。
* Revision 递增。
* 错误 transaction 不污染 Buffer。

#### `tests/m3_history.rs`

测试：

* 单步 undo。
* 单步 redo。
* 多步 undo。
* 多步 redo。
* undo boundary。
* redo stack 清理。
* transaction merge 的 public 行为。

#### `tests/m4_selection.rs`

测试：

* Selection 创建。
* Selection normalize。
* 多 selection merge。
* 多光标插入。
* 多光标删除。
* 重叠 selection 行为。

#### `tests/m5_coordinates.rs`

测试：

* ByteOffset <-> Line / Column。
* UTF-16 Position。
* CRLF 边界。
* Tab Width。
* Display Column。

#### `tests/m6_unicode.rs`

测试：

* emoji。
* ZWJ emoji。
* 组合音标。
* 中文。
* 日文。
* 韩文。
* grapheme-safe delete。

#### `tests/m7_search_replace.rs`

测试：

* 普通搜索。
* 正则搜索。
* whole word。
* range search。
* replace。
* replace all as transaction。

---

## 14. CI 建议

核心 CI 建议运行：

```bash
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

后续可加入：

```bash
cargo test --test m0_domain_model
cargo test --test m1_buffer
cargo test --test m2_transaction
cargo bench
```

GPUI testbed 不建议默认进入核心 CI，除非有稳定的 GUI 测试环境。

```text
examples/ 默认不进 CI
examples/ 可以作为本地手动验收入口
examples/ 后期可以增加少量 headless smoke test，但不作为核心正确性来源
```

---

## 15. 最终原则

* [ ] 核心编辑算法必须在纯测试中验证。
* [ ] 默认优先把自动化正确性测试放在根目录 `tests/`。
* [ ] `tests/` 负责 public API 机器契约。
* [ ] `examples/` 负责 GPUI testbed 和人类体感。
* [ ] `src/tests/` 是可选内部测试区，只测试 public API 覆盖不到的重要 `pub(crate)` 不变量。
* [ ] 不为了测试污染 public API。
* [ ] 不为了覆盖率锁死短期实现细节。
* [ ] UI 测试只验证桥接，不重复测试算法。
* [ ] 性能测试放入 `benches/`。
* [ ] 高性能存储必须与简单 String 参考模型做差分测试。
* [ ] Unicode、CRLF、Grapheme、Display Column 必须有专门测试。
* [ ] Undo / Redo 必须测试文本和 selection 双恢复。
* [ ] GPUI testbed 只承担“水面之上”的 20% 验证。

最终一句话：

```text
默认 tests/，UI examples/，性能 benches/，只有重要内部不变量才 src/tests/。
```
