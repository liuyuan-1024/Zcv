# zom 目标架构设计

> 本文定义 `zom` 破坏性架构重构的目标状态、子系统边界、依赖方向和完成标准。
> 本次重构不保留旧 API、旧模块路径、兼容层或迁移期双实现。

本文是目标架构、长期边界和重构验收标准的唯一规范来源。`README.md` 在重构完成前可以描述当前实现，但不得定义另一套目标架构；crate 文档只能细化本文件分配给该 crate 的职责，不能改变依赖方向、状态所有权或执行入口。

## 1. 设计目标

`zom` 的目标不是简单增加 crate 数量，而是建立可以独立理解、独立测试、独立替换的黑盒子系统。

一个合格的黑盒子系统必须满足：

- 独占自己的领域状态，不与其他 crate 共享可变内部对象。
- 只暴露完成当前职责所需的最小 public API，不强制套统一接口模板。
- 不向调用方返回内部状态的可变引用。
- 不泄漏 GPUI、Tree-sitter、`lsp-types` 等第三方实现类型。
- 不依赖自己的调用方，不反向访问组合根或 UI 内部模块。
- 自己管理后台任务、取消、版本校验和过期结果。
- public API 只表达长期领域语义，不暴露源码目录结构。

架构判断优先级：

1. 状态只能有一个所有者。
2. 一个业务能力只能有一个权威执行入口。
3. 依赖必须从组合层指向能力层，不能形成环。
4. 第三方实现细节必须被收纳在所属子系统内部。
5. 只有存在真实职责和真实消费方时才建立抽象。

当前不设计插件系统，也不为未知的第三方调用方预留接口。命令、面板、语言支持和应用能力都以当前内建需求为边界；只有出现至少两个真实消费者时，才提取共享抽象。

## 2. 目标 workspace

目标 workspace 由以下运行期 crate 组成：

```text
zom-desktop
    │
    ▼
zom-app
    ├── zom-command
    ├── zom-editor ──────► zom-engine
    ├── zom-project
    ├── zom-language ────► zom-engine
    └── zom-vcs

zom-bench ──► zom-engine / zom-editor / zom-language
```

依赖规则：

- 在产品运行路径中，`zom-app` 是唯一允许同时认识多个业务子系统的 crate；`zom-bench` 只作为测量入口直接调用被测 public API。
- `zom-desktop` 在内部 crate 中只依赖 `zom-app`。
- `zom-command` 不依赖任何业务 crate。
- `zom-editor` 和 `zom-language` 可以依赖纯文本底座 `zom-engine`。
- `zom-app` 可以依赖 `zom-engine` 以使用其纯值类型，但不持有 `zom-engine` 的实例状态。
- `zom-project` 和 `zom-vcs` 互不依赖，由 `zom-app` 编排。
- `zom-bench` 是测量工具，不参与运行期依赖图。

## 3. 子系统职责

### 3.1 `zom-engine`：纯文本算法内核

拥有：

- 文本存储与切片。
- 坐标、range、position map 和 anchor。
- 事务、delta、undo/redo 和历史。
- selection、fold、projection 等可复用值类型和编辑算法。
- 文本编码、换行和大文件基础策略。

不拥有：

- 文件路径和项目根。
- 打开的文档或视图集合。
- 活动标签、UI 焦点和面板状态。
- Tree-sitter、LSP、命令或 GPUI。

它是无运行时、无宿主知识的基础库，不负责应用级对象标识。它可以定义 `TextVersion` 这类纯文本版本值，但只定义值和算法；某个打开文档当前处于哪个版本，仍由 `zom-editor` 唯一决定。selection、fold 等实例状态同样属于 `zom-editor`，`zom-engine` 只拥有其数据结构与变换算法。

### 3.2 `zom-editor`：编辑域

拥有：

- `DocumentId`、`ViewId` 及其分配。
- 打开文档和草稿。
- 文档脏状态、只读状态和保存版本。
- 编辑视图、预览视图和活动视图。
- 每个视图独有的 selection、fold、viewport、软换行和 reveal 状态。
- 单文档搜索、替换和 IME composition。
- 对 `zom-engine::Buffer` 的唯一应用级所有权。

对外提供 `Editor` facade、强类型编辑方法、面向渲染的不可变 viewport snapshot 和领域错误。同步编辑直接返回结果；只有真实后台任务才产生事件。

禁止对外提供 `&mut Buffer`、`&mut ViewSet` 或其他内部对象。

`zom-editor` 不保存项目文件的 canonical path。`DocumentId` 与 `zom-project::FileId` 的对应关系是跨子系统关系，由 `zom-app` 唯一持有；文件改名只更新 `zom-project` 内的路径映射，不需要复制修改 editor 状态。

### 3.3 `zom-project`：项目文件系统

拥有：

- 当前项目根及 project epoch。
- `FileId` 到 canonical path 的唯一映射。
- 每个文件当前磁盘内容的 `FileRevision`。
- 文件树和忽略规则。
- 文件创建、移动、重命名、删除和读取、写入边界。
- 文件监听和外部变化归一化。
- 项目范围的文件与文本搜索。

对外提供 `Project` facade、文件与项目树方法、文件树快照和领域错误。文件读取返回带 `FileRevision` 的不可变快照；写入、移动和删除接受预期 revision 或 project epoch，不匹配时返回冲突而不是覆盖未知的新状态。文件监听是实际异步源，因此单独产生 `ProjectEvent`。

它不知道编辑器当前打开了哪些文档。外部文件变化如何影响已打开文档，由 `zom-app` 协调 `zom-project` 与 `zom-editor`。

### 3.4 `zom-language`：语言智能

拥有：

- 语言识别和语言注册表。
- Tree-sitter parser、query、注入和后台 worker。
- LSP server 进程、JSON-RPC transport 和生命周期。
- 文档同步、syntax highlights、semantic highlights 和 diagnostics。
- 语言服务能力与 server 状态。

对外提供 `LanguageService` facade、高亮与诊断快照、后台完成事件和领域错误。语言分析本身是异步能力，因此在这里保留明确的事件边界。

`tree_sitter::*`、`lsp_types::*` 和原始 JSON-RPC 值都只能存在于本 crate 内部。公开高亮、诊断和位置类型必须是 `zom` 自己的领域类型。

`zom-language` 分配自己的 `LanguageSessionId` 和 `LanguageDocumentId`，不导入 `zom-editor::DocumentId` 或 `zom-project::ProjectEpoch`。`zom-app` 在打开语言文档时建立跨子系统关联，并把 editor 提供的只读文本快照及 `zom-engine::TextVersion` 交给 language。

所有异步产物必须携带 `LanguageSessionId`、`LanguageDocumentId` 和 `TextVersion`。language 只发布当前 session、当前文档最新输入版本的结果，过期结果在 `zom-language` 边界内丢弃，不能交给 app 或 UI 判断。切换项目时创建新的 language session，旧 session 的全部结果无条件失效。

### 3.5 `zom-vcs`：版本控制

拥有：

- repository 状态和当前 branch。
- 工作区文件状态、stage/unstage 和 diff 状态（包括 hunk 生成与缓存）。
- ahead/behind 计数和 branch 列表。
- fetch、pull、push、checkout、merge、rebase、stash 等后台操作。
- 冲突状态和冲突文件列表。
- 操作 generation、取消和错误归一化。

对外提供 `Vcs` facade、repository 快照、后台操作完成事件和领域错误。

Git 命令、进程输出和实现细节不能泄漏给 `zom-app` 或 `zom-desktop`。

`zom-vcs` 当前按 Git 集成范围定义条目，不做推测性扩展。新增操作类型由真实消费方需求驱动。

### 3.6 `zom-command`：命令描述与输入解码

只负责：

- `CommandId` 和命令元数据。
- 编译期确定的内建 command catalog。
- key chord、key sequence 和上下文匹配。
- keymap 加载、冲突检测和解析。
- 配置字符串到 typed command 的边界解码机制。

不负责：

- 持有或借用 workspace、view、buffer、clipboard、panel 或 Git 状态。
- 执行业务 handler。
- 定义混合所有功能的 `HostEffect`。
- 充当全局 service locator。
- 提供运行时插件注册、类型擦除 handler 或动态扩展点。

键位和配置中的字符串命令进入 `zom-app` 时只解码一次。应用内部使用闭合的强类型 `AppCommand`；新增内建命令直接修改 enum、catalog 和穷尽匹配，不为未知命令保留动态分支。

### 3.7 `zom-app`：无头应用内核与组合根

拥有：

- 当前是否持有 project 实例及其生命周期，但不复制 project root、epoch 或文件树状态。
- `DocumentId`、`FileId`、`LanguageDocumentId` 等跨子系统标识之间的唯一关联。
- 语义焦点和活动 surface。
- 面板、弹层、配置来源合并结果和生效设置等非像素级应用状态。
- typed command 的唯一派发入口。
- editor、project、language 和 VCS 之间的用例编排。
- 面向渲染端的不可变 `AppSnapshot`。

核心边界：

```text
Application::dispatch(AppInput) -> AppUpdate
Application::advance() -> AppUpdate
Application::snapshot() -> AppSnapshot
Application::complete(PlatformCompletion) -> AppUpdate
```

`Application` 内部拥有一个有界事件收件箱。project、language 和 VCS 的事件只进入这个收件箱，并发出合并后的 wake signal；desktop 收到 wake signal 后调用 `advance()` 处理有限批次，而不是每帧轮询。收件箱是 `zom-app` 的私有编排机制，不是公开的通用事件总线，也不允许子系统借此互相通信。

`PlatformCompletion` 必须携带对应的 platform request ID。迟到、重复或已经取消的完成结果由 `zom-app` 丢弃，不能再次提交应用状态。`AppUpdate` 只描述需要执行的平台请求和已经变化的 snapshot slice revision，不保存另一份应用状态。

`zom-app` 不依赖 GPUI，不公开 `Workspace`、`ViewSet` 或任何子系统内部状态。

`zom-app` 是外部输入的组合根，不是所有业务规则的实现容器。子系统内部动作必须立即委托给对应 facade；只有同时涉及两个或更多子系统的用例、跨子系统关联和应用级决策才由 `zom-app` 自己实现。

### 3.8 `zom-desktop`：GPUI 与平台实现

拥有：

- GPUI application、window 和 element。
- GPU 资源、字体度量、主题投影和像素布局。
- hover、拖拽、resize、scroll handle 等纯视觉瞬态。
- OS 键盘、指针和 IME 事件解码。
- 原生窗口操作、文件选择器、剪贴板和平台差异。

它只做三件事：

1. 把 OS 事件转换为 `AppInput`。
2. 根据 `AppSnapshot` 渲染。
3. 执行文件选择器、剪贴板和窗口操作等明确的 `PlatformEffect`，并将对应结果送回 `Application::complete`。

desktop 不得直接访问 engine、editor、project、language 或 VCS。

`PlatformEffect` 只表达必须由 OS 或 GPUI 完成的平台请求，不能承载编辑、项目、语言或 VCS 业务动作，也不能演变成新的通用 `HostEffect`。
每个需要结果的 effect 都携带 platform request ID；无需结果的窗口通知不能伪装成业务完成事件。

## 4. 唯一执行入口

所有用户触发的离散动作最终转换为 typed `AppCommand`：

```text
快捷键 ───┐
菜单 ─────┤
按钮 ─────┤
命令面板 ─┼──► AppCommand ───► Application::dispatch
```

`Application::dispatch` 是 desktop 和其他外部输入进入应用的唯一入口。不同触发源只负责收集参数和构造命令，不能拥有业务语义。

键位配置由 `zom-command` 解析为 `(CommandId, CommandArgs)`，`zom-app` 在边界处将它解码为闭合的 `AppCommand`。按钮、菜单和命令面板直接构造同一个 `AppCommand`，随后全部进入 `Application::dispatch`。不存在动态 handler 注册或第二条业务执行路径。

字符串 `CommandId` 只服务于配置和序列化边界。业务执行不能依赖字符串字段查找和运行时拼装参数。

唯一外部入口不等于唯一业务实现。每项语义仍由其状态所有者提供唯一权威方法：编辑语义在 `Editor`，文件语义在 `Project`，语言语义在 `LanguageService`，VCS 语义在 `Vcs`。`Application::dispatch` 只做输入校验、用例路由和必要的跨子系统编排，禁止复制这些语义形成第二套实现。

## 5. 黑盒通信规则

黑盒不要求接口形式一致。按当前需求选择最直接的表达：

- 同步状态变更使用普通强类型方法并直接返回结果。
- 渲染需要稳定读模型时才提供不可变 Snapshot。
- 文件监听、语言分析和 Git 后台操作等真实异步源才产生 Event。
- 不为只调用一次的方法创建 Request enum，不为同步结果创建事件总线。
- 第三方错误和进程输出在所属子系统内转换成调用方能够处理的领域错误。

通用规则：

- ID 由其所有者 crate 定义和分配。
- 不公开通用 `from_raw` 构造入口。
- 跨子系统不能借用另一个子系统的实体 ID 假装是自己的 ID；关联关系由 `zom-app` 显式维护。
- 纯值类型可以由依赖图中的共同底座定义。例如 `TextVersion` 由 `zom-engine` 定义，editor 是当前值的权威所有者，language 只能把它作为输入版本标签使用。
- 禁止跨 crate 共享 `Rc<RefCell<T>>` 或 `Arc<Mutex<T>>` 领域状态。
- 禁止跨 crate 返回内部对象的 `&mut` 引用。
- 禁止 UI 轮询内部 worker 状态。
- 后台结果通过 app 私有收件箱和合并 wake signal 推进，只在收到唤醒后调用 `advance()`，不依赖每帧扫描所有服务。
- 所有后台操作携带其所有者定义的 operation ID、version、generation 或 session ID。

## 6. 单一数据源与状态归属

任何可变事实都必须明确一个唯一所有者和唯一写入入口。其他层只能保存不可变投影、标识关联或可丢弃缓存，不能维护第二份可写副本。

状态归属按以下问题判断：

```text
这是文本算法本身吗？                    → zom-engine
这是打开文档或某个编辑视图的状态吗？    → zom-editor
这是项目磁盘和文件树的事实吗？          → zom-project
这是语言分析或 language server 的结果吗？ → zom-language
这是 repository 的事实吗？             → zom-vcs
这是跨子系统的应用决策吗？              → zom-app
这是像素、窗口或 OS 资源吗？            → zom-desktop
```

同一状态只能存在一个真相源。其他子系统需要展示时使用带版本的投影或 Snapshot，不能维护第二份可写副本。

### 6.1 权威状态表

| 状态 | 唯一数据源 | 其他层允许持有的内容 |
|---|---|---|
| 文本、历史、dirty、save version | `zom-editor` | 指定 document version 的只读文本或 viewport snapshot |
| 活动 view、selection、fold、viewport | `zom-editor` | `ViewId` 和带 revision 的渲染投影 |
| project root、project epoch、文件存在性、canonical path 和 `FileRevision` | `zom-project` | `FileId` 和最后观察到的 revision；不得复制维护 path map |
| 跨子系统 ID 关联和子系统实例关联 | `zom-app` | 子系统各自只知道自己的 ID；不得自行推导另一领域的 ID |
| 语义焦点、panel、surface、跨子系统冲突和进行中的应用用例 | `zom-app` | desktop 中的只读 UI 投影 |
| 合并后的生效设置和 `SettingsRevision` | `zom-app` | 各子系统持有对应 revision 的派生配置 |
| 编译后的 keymap | `zom-command` | 它是 settings 指定版本的可重建投影，不是第二份配置源 |
| syntax、semantic highlights、diagnostics、language server 状态 | `zom-language` | 带 language session 和 text version 的只读结果 |
| branch、status、ahead/behind、Git operation 状态 | `zom-vcs` | 带 repository generation 的只读投影 |
| 像素布局、hover、GPU 资源和窗口句柄 | `zom-desktop` | 不向领域层反向同步为业务状态 |

### 6.2 投影和缓存规则

允许缓存，但缓存不是数据源，必须同时满足：

- 明确标注来源 revision、document version、project epoch 或 repository generation。
- 来源版本不匹配时直接失效或重算，不能尝试双向合并。
- 缓存没有独立的业务写入 API。
- 删除缓存不改变任何业务语义。
- `AppSnapshot` 和 desktop view model 都是只读投影，不能被回写。
- 每个 snapshot slice 有独立 revision；渲染端根据 revision 判断变化，不比较或复制整棵状态树。
- `AppSnapshot` 是逻辑上的组合视图，可以内部共享不可变数据，不要求每次更新深拷贝全量应用状态。

如果一个字段需要在两个结构体中同步赋值，应优先删除其中一份，让消费方查询权威所有者或重新生成投影。

### 6.3 配置单一数据源

配置遵循同样的所有权规则：默认配置、用户配置和项目配置进入 `zom-app` 后完成一次合并，形成唯一的生效设置与 `SettingsRevision`。其他子系统不能各自读取同一配置文件或再次执行合并优先级。

派生流程固定为：

```text
配置来源
   ↓
zom-app 合并并发布 SettingsRevision
   ├──► zom-command 编译 keymap
   ├──► zom-editor 应用编辑设置（制表符宽度、缩进样式、软换行默认值等）
   ├──► zom-language 应用语言设置
   └──► zom-desktop 生成主题和字体投影
```

- `zom-command` 负责 keymap 的解析、校验和冲突检测，但不拥有原始配置来源和合并优先级。
- `zom-editor` 只保留执行编辑操作和生成 viewport snapshot 所需的派生设置及对应 revision。
- `zom-language` 只保留启动 server 和分析所需的派生设置及对应 revision。
- `zom-desktop` 只保留渲染所需的主题、字号和平台投影及对应 revision。
- 新配置解析失败时，旧的有效 revision 保持不变；错误由 `zom-app` 作为应用状态呈现，不能让各子系统分别回退到不同配置。

### 6.4 跨子系统标识

每个实体 ID 只在所属子系统内有意义：

```text
zom-editor   → DocumentId / ViewId
zom-project  → FileId / ProjectEpoch
zom-language → LanguageSessionId / LanguageDocumentId
zom-vcs      → RepositoryId / OperationId
```

`zom-app` 只维护真实存在的关联，例如：

```text
DocumentId ↔ FileId
DocumentId ↔ LanguageDocumentId
当前 Project 实例 ↔ LanguageSessionId ↔ RepositoryId
```

关联不是实体状态的副本。文件路径只能通过 `FileId` 向 project 查询，文本只能通过 `DocumentId` 向 editor 查询，诊断只能通过 `LanguageDocumentId` 向 language 查询。app 可以在文档关联上记录“该文档最后观察到的 `FileRevision`”作为跨系统并发令牌，但不能据此修改或伪造 project 的当前 revision。关闭实体时必须在同一应用用例内删除相关关联，禁止留下悬空映射。

### 6.5 跨子系统事务

跨子系统操作由 `zom-app` 编排，但状态仍由各自所有者提交，`zom-app` 不复制结果成为新数据源。

不建立通用事务框架。每个真实的跨子系统用例使用显式状态机，逐项定义提交顺序、失败语义和版本校验：

| 用例 | 权威顺序与不变量 |
|---|---|
| 打开文件 | project 成功读取带 `FileRevision` 的文件快照后 editor 才创建文档；app 随后记录关联与观察到的 revision，并打开 language document。language 不可用时编辑器仍可进入明确的降级状态，不回滚已经成功打开的文本。 |
| 关闭文档 | app 先解决 dirty 确认，再让 language 取消该文档任务，删除关联，最后由 editor 关闭文档；取消确认时任何状态都不变化。 |
| 保存文件 | editor 生成指定 `TextVersion` 的只读保存快照；project 仅在 `FileRevision` 仍等于 app 最后观察值时原子写入并返回新 revision；app 更新关联后，editor 仅在文本版本仍匹配时推进 save version。任一版本冲突都不得覆盖未知状态。 |
| 重命名或移动文件 | project 在预期 project epoch 仍有效时原子修改路径并保持 `FileId` 稳定；editor 不保存路径所以无需更新。app 仅在语言识别确实受扩展名影响时更新 language 输入。 |
| 删除文件 | project 删除成功后 app 移除文件关联；仍打开的 editor 文档转为无文件关联的 dirty 草稿，文本不能丢失。删除失败时关联和文档均不变化。 |
| 外部文件变化 | project 归一化 watcher 事件；app 对照 editor dirty 和版本决定重载或建立冲突状态。watcher 永远不能直接写入 editor。 |
| Git checkout 等工作树修改 | VCS 完成操作后 project 执行权威刷新并推进 epoch，所有文件变化统一进入外部变化流程；VCS 不直接修改 editor 或 project snapshot。 |
| 切换项目 | app 先解决所有 dirty 文档，再取消旧 language/VCS 操作和 watcher，关闭旧实例、清除关联，最后创建新 project、language session 和 repository；不允许新旧项目同时成为当前项目。 |

保存文件的权威流程：

```text
zom-editor 生成指定 TextVersion 的保存快照
    ↓
zom-app 根据 DocumentId ↔ FileId 关联取得最后观察到的 FileRevision
    ↓
zom-project 仅在 FileRevision 匹配时执行原子写入并返回新 FileRevision
    ↓
zom-app 更新关联中的观察 revision
    ↓
zom-editor 仅在 TextVersion 仍匹配时推进 save version
```

`TextVersion` 防止保存期间的新编辑被错误标记为已保存，`FileRevision` 防止保存覆盖尚未处理的外部磁盘修改。磁盘外部修改由 `zom-project` 产生事件，`zom-app` 根据 editor 的 dirty、save version 和最后观察到的 file revision 决定 reload 或进入冲突状态，不能让 watcher 直接覆盖 buffer。

跨子系统用例的进行中状态由 `zom-app` 唯一拥有，并携带 application operation ID。失败时不做猜测性的通用回滚，而是按上表保留已经形成的有效状态，并执行该用例明确规定的补偿或降级行为。

### 6.6 生命周期与并发

- `zom-app` 创建并拥有 editor、project、language 和 VCS facade；子系统 facade 自己拥有其后台任务和内部并发状态。
- 同步编辑和纯状态变更在调用返回前完成，不为了接口统一转成后台消息。
- 文档关闭、项目切换和应用关闭必须显式取消所属后台操作；仅依赖 Rust `Drop` 不算完整的生命周期设计。
- language 使用 language session 和 text version，project 使用 project epoch，VCS 使用 repository generation 和 operation ID；旧 generation 的完成结果在所有者边界内丢弃。
- 后台事件通道必须有界。可以被新状态完全替代的通知只保留最新值；不可丢失的操作完成结果使用背压，不能静默丢弃。
- watcher 溢出或事件连续性无法证明时，project 必须执行完整重扫并推进 epoch，不能把不完整增量当成真实文件树。
- lifecycle 控制方法必须可重复调用；取消、关闭或迟到完成事件不能导致二次提交。
- 应用关闭顺序固定为：停止接收新输入，解决或取消应用用例，停止 language 与 VCS 后台操作，停止 project watcher，关闭 editor 文档，最后由 desktop 释放窗口和平台资源。

### 6.7 故障隔离

| 故障 | 必须保持的行为 |
|---|---|
| 文件读取或写入失败 | project 不发布成功状态；editor 原文本和 dirty 状态保持不变。 |
| watcher 溢出或失联 | project 标记快照不可继续增量信任，完成全量重扫后才发布新 epoch。 |
| parser、语言 worker 或 language server 崩溃 | language 进入可观察的降级状态并管理自己的重启；editor、project 和应用输入继续可用。 |
| Git 命令失败或被取消 | VCS 保留最后一个已确认 repository snapshot，并发布领域错误；project 和 editor 不接受半成品状态。 |
| 平台请求失败 | desktop 返回明确的 `PlatformResult`，app 结束对应应用用例并保持领域状态一致。 |

每个子系统定义自己的领域错误，不建立跨所有领域的万能错误枚举。重试机制属于发生故障的子系统；是否发起某个用户可见用例仍由 `zom-app` 决定。内部不变量被破坏属于程序缺陷，不能伪装成可恢复业务错误或通过 warning 抑制隐藏。

## 7. 现有结构的破坏性处理

### 7.1 删除 `zom-workspace`

当前 `zom-workspace` 同时承担文档、view、项目树、搜索、语法和后台 worker，不是单一能力黑盒。

拆分方向：

```text
文档生命周期 / view / buffer search → zom-editor
project tree / 文件操作 / 文件监听  → zom-project
syntax / provider / worker          → zom-language
```

迁移全部调用方后删除 crate，不保留旧模块、类型别名或 re-export。

### 7.2 删除 `zom-lsp`

将以下实现收拢到 `zom-language`：

- 当前 `zom-lsp` 协议与 transport。
- desktop 的 `LspHost`。
- workspace 的 semantic token 转换。
- diagnostics 和 server 状态。

完成后删除 `zom-lsp`，其他 crate 不再直接依赖 `lsp-types`。

### 7.3 重做 `zom-command`

删除：

- `CommandContext`。
- 业务 handler registry。
- 对 `Workspace`、`ViewSet` 和 `Buffer` 的借用。
- `HostEffect` 业务枚举。
- effect 发出后回到 App 修改领域状态的往返路径。

保留并重建为纯输入基础设施：command catalog、keymap、命令元数据和外部输入解码。

### 7.4 瘦身 `zom-desktop`

迁出：

```text
App / WorkspaceSession / 语义焦点 / 配置编排 → zom-app
GitService                                  → zom-vcs
FileWatcherService                          → zom-project
LspHost                                     → zom-language
```

desktop 最终不能公开或借用任何领域容器。

## 8. 禁止兼容层

本次重构明确禁止：

- 旧模块 re-export。
- 旧类型别名。
- deprecated API。
- forwarding wrapper。
- shim、legacy feature 或兼容分支。
- 新旧状态双写。
- 新旧事件双发。
- 为旧测试保留的 public API。
- 旧配置格式的隐式兼容读取。
- 通过 `#[allow(...)]`、`#![allow(...)]` 或 workspace lint 降级隐藏 warning。

每次边界切换必须在同一个完整变更中完成：

```text
建立新入口
  → 迁移全部调用方
  → 删除旧入口和旧实现
  → 删除旧测试
  → 建立新 public contract 测试
```

未删除旧入口的迁移不算完成。

## 9. 测试边界

### `zom-engine`

- 源码旁单元测试验证算法不变量。
- property test 验证事务、坐标映射和历史。
- crate 集成测试只使用 public API。

### `zom-editor`

- 文档和视图生命周期契约。
- 同文档多视图的独立状态。
- 强类型方法与版本化 snapshot/event 的契约。
- 不通过 public API访问内部 Buffer。

### `zom-project`

- 临时目录中的文件操作契约。
- watcher 事件归一化和 project epoch。
- `FileRevision` 条件写入、移动和删除的冲突契约。
- watcher 溢出后的全量重扫契约。
- 搜索和忽略规则。

### `zom-language`

- parser 和 query 的模块内测试。
- fake transport 下的 LSP 生命周期。
- language session、text version 与过期结果丢弃契约。
- server 崩溃后的故障隔离和重启契约。
- public API 不出现 Tree-sitter 和 LSP 类型。

### `zom-vcs`

- 临时 repository 契约。
- generation、并发操作和失败归一化。
- 失败或取消后仍保留最后确认 snapshot 的契约。

### `zom-app`

- 无 GPUI 的完整用例测试。
- 所有触发源汇入同一 typed command 的契约。
- 打开、关闭、保存、改名、删除、外部变化、Git 工作树变化和切换项目的跨子系统契约。
- 跨子系统关联、事件编排和状态投影。
- settings revision 与各派生配置一致性。
- platform effect 请求与结果回注。

### `zom-desktop`

- 输入解码和渲染投影测试。
- snapshot slice revision 驱动的增量渲染测试。
- GPUI harness 下的外部可观察交互。
- 不重复测试领域业务规则。

不得为了测试提升 private API 的可见性。

## 10. 实施顺序

### 阶段一：建立应用内核和新命令入口

- 新建 `zom-app`。
- 把 desktop 中的应用状态、语义焦点和派发入口迁入。
- 建立 typed `AppCommand`。
- 把 `zom-command` 改为无业务依赖的输入基础设施。
- 更新 desktop 调用方并立即删除旧执行器和旧 effect 路径。

### 阶段二：建立编辑黑盒

- 新建 `zom-editor`。
- 迁移文档、view、搜索和 IME 状态。
- 让 `zom-app` 只通过 `Editor` facade 的强类型方法、Snapshot 和真实异步 Event 使用它。
- 删除 desktop 和 command 对 engine/workspace 内部状态的访问。

### 阶段三：建立项目黑盒

- 新建 `zom-project`。
- 迁移 project tree、文件操作、watcher 和项目搜索。
- 由 `zom-app` 编排外部文件变化与 editor 文档状态。

### 阶段四：建立语言黑盒

- 新建 `zom-language`。
- 合并 syntax、LSP、semantic token 和 diagnostics。
- 建立 `DocumentId ↔ LanguageDocumentId` 关联以及 language session/text version 失效规则。
- 删除 `zom-lsp` 和其他 crate 对 `lsp-types` 的依赖。

### 阶段五：建立 VCS 黑盒

- 新建 `zom-vcs`。
- 迁移 GitService、branch 和后台操作。
- 删除 desktop 中的 repository 状态副本。

### 阶段六：删除旧结构

- 删除 `zom-workspace`。
- 删除 `zom-lsp`。
- 删除 desktop 中已迁出的 app/service 模块。
- 删除全部旧 public path、旧测试和旧文档描述。
- 以本文作为唯一目标架构规范，不保留另一份并行架构原则。
- 收紧依赖图和 public API。

## 11. Warning 与架构门禁

不得通过 lint 配置隐藏 warning。现有 lint `allow` 必须删除并修复产生它的源码或文档。

最终持续集成至少执行：

```bash
cargo fmt --all -- --check
RUSTFLAGS="-D warnings" cargo check --workspace --all-targets --all-features
cargo clippy --workspace --all-targets --all-features -- -D warnings
RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps
cargo test --workspace --all-features
```

另外增加依赖图门禁：

- desktop 的内部依赖只能是 app。
- command 不能依赖业务 crate。
- 除 app 外，业务子系统不能互相依赖。
- engine 不能依赖任何宿主层 crate。
- GPUI 只能出现在 desktop。
- Tree-sitter 和 `lsp-types` 只能出现在 language。
- Git 实现依赖只能出现在 vcs。
- `notify` 等项目文件监听依赖只能出现在 project。
- 禁止目标架构之外的文档重新定义 crate 依赖、状态所有权和执行入口。

## 12. 完成标准

只有同时满足以下条件，架构重构才算完成：

- `zom-desktop` 在内部 crate 中只依赖 `zom-app`。
- `zom-command` 不依赖 engine、editor、project、language 或 VCS。
- `zom-workspace` 和 `zom-lsp` 已从 workspace 删除。
- 所有 desktop 和用户触发动作只通过 `Application::dispatch` 进入应用。
- 每项领域语义只在其状态所有者中实现一次，`zom-app` 不复制子系统业务规则。
- 每个子系统只暴露当前真实调用方需要的最小 public API。
- 每项可变状态都有唯一所有者和唯一写入入口，不存在跨 crate 双写字段。
- 每类实体 ID 只由所属子系统分配，跨子系统关联只由 `zom-app` 持有。
- 所有关键跨子系统用例都有明确的提交顺序、失败语义和版本校验。
- 保存同时校验 editor 的 `TextVersion` 和 project 的 `FileRevision`，不会误标新编辑或覆盖外部修改。
- 后台子系统具有明确的取消、过期结果丢弃、故障降级和 shutdown 契约。
- 生效设置只有一个 `SettingsRevision`，keymap、language 和 desktop 配置都是该版本的可重建派生结果。
- 所有投影和缓存都携带来源版本，且删除缓存不改变业务语义。
- `AppSnapshot` 按 slice 修订，不要求每次更新深拷贝全量状态。
- UI 无法取得任何领域状态的可变引用。
- 不存在跨 crate 共享的可变领域容器。
- public API 不泄漏第三方协议和框架类型。
- 不存在 deprecated、旧名称 re-export、legacy feature 或双实现。
- 不存在任何为了隐藏 warning 添加的 lint `allow`。
- format、check、clippy、rustdoc 和 test 全部零 warning 通过。
- 本文是唯一目标架构规范，README、crate 文档和实际 Cargo 依赖图在重构完成时与本文完全一致。

这份目标架构是重构验收基线。实现过程中如果某项改动无法满足这些边界，应调整实现，而不是降低边界或增加兼容代码。
