# CODEX.md

> 本文记录全局 agent 工作约定、项目结构、验证命令和代码风格。

## 修改项目代码时必须遵守的规范

1. 修改前，请先阅读该 crate 根目录及docs目录下的所有文档规范等。
2. 查阅**本机的/Users/liuyuan/project/liuyuan/zed目录下的Zed编辑器的源码**，看看zed是如何实现的，参考zed的实现方式，甚至是直接照搬zed的实现方式。
3. 开发时不要兼容旧代码、不要双轨代码，保证架构、代码是最新最强的状态。
4. 不要使用#[allow(dead_code)]等方式抑制警告。
5. zcv不考虑实时协作、远程操作。

## 称呼

在每次回答问题的时候，请加上一个称呼：“伟大的开发者”。

## 编码准则

本节整合自 `CLAUDE.md`，用于减少 LLM 常见编码错误。执行时应与项目特定说明一起遵守。

**权衡：** 这些准则倾向于谨慎而非速度。对于简单的任务，请自行判断。

### 1. 编码前思考

**不要想当然。不要掩盖困惑。主动说明取舍。**

实施前：
- 明确陈述你的假设。如有疑问，请提出。
- 如果存在多种解释，请列出它们，不要默默选择。
- 如果存在更简单的方法，请说明。必要时提出异议。
- 如果有任何不清楚的地方，请停止。指出困惑之处。提出问题。

### 2. 简洁至上

**只写解决问题所需的最小代码。不做推测性实现。**

- 不添加超出要求的功能。
- 不为一次性使用的代码编写抽象。
- 不添加任何未经请求的“灵活性”或“可配置性”。
- 不对不可能的情况进行错误处理。
- 如果你写了 200 行代码，而它其实可以简化成 50 行，那就重写。

问问自己：“一位资深工程师会觉得这段代码过于复杂吗？” 如果会，那就简化它。

### 3. 精准修改

**只修改必要的部分。只清理你自己造成的混乱。**

编辑现有代码时：
- 不要“改进”相邻的代码、注释或格式。
- 不要重构没有问题的代码。
- 保持与现有代码风格一致，即使你的想法不同。
- 如果你发现无关的无用代码，请指出，不要删除它。

当你的修改导致孤立代码出现时：
- 删除你修改后不再使用的导入/变量/函数。
- 除非被要求，否则不要删除已有的无用代码。

测试：每一行修改都应该直接追溯到用户的请求。

### 4. 目标驱动执行

**定义成功标准。循环执行直至验证通过。**

将任务转化为可验证的目标：
- “添加验证” → “编写无效输入的测试，并确保测试通过”
- “修复错误” → “编写可复现该错误的测试，并确保测试通过”
- “重构 X” → “确保重构前后的测试均通过”

对于多步骤任务，请简要说明计划：

```
1. [步骤] → 验证：[检查]
2. [步骤] → 验证：[检查]
3. [步骤] → 验证：[检查]
```

严格的成功标准允许你独立循环执行。较弱的标准，例如“使其正常工作”，则需要不断澄清。

## 协作规范

### 1. 语言规范

中文是本项目协作中的第一语言。

默认使用中文的内容包括：

```text
提交信息
代码注释
文档
测试说明
错误提示、日志、诊断信息等面向人的字符串
CLI 输出、UI 文案、解释性字符串
PR / review / issue / changelog 描述
AI 回复与修改说明
```

允许保留英文的内容包括：

```text
Rust 标识符、类型名、trait 名、函数名、字段名、模块名
crate / package / binary 名称
协议名、标准名、错误码、环境变量、命令行参数
第三方 API、上游术语、精确引用
必须与外部生态保持一致的固定文本
```

需要同时照顾可读性和检索性时，优先使用“中文说明 + 英文术语”的形式，例如：

```text
事务基准版本（base_version）不匹配。
```

不要为了中文化而强行翻译代码符号、公共 API 名称或外部协议术语。

### 2. Git 规范

提交信息使用中文，尽量用一句话总结改动，保持简练、具体、可检索。

优先使用一个动宾短语，说明“做了什么”；不要在提交信息里展开背景、过程或多句说明，必要细节放到回复、PR 或 changelog 中。

推荐格式：

```text
接入 zcv-engine
补充协作规范
修复事务版本校验
```

避免使用空泛提交信息：

```text
update
fix
wip
调整
```

### 3. 注释规范

注释用于补充代码本身无法清晰表达的信息，优先解释为什么这样做，而不是重复代码已经表达的做了什么。

应当添加注释的情况：
- 存在不直观的业务规则、边界条件或历史原因。
- 为了兼容平台、上游库、协议、旧数据或既有行为而采用了看似奇怪的写法。
- 代码中存在性能、安全、并发、生命周期、编码、事务一致性等容易被误改的约束。
- 存在临时方案、折中方案、已知限制或后续需要清理的地方。
- 公共 API、模块边界、复杂数据结构或容易误用的函数，需要说明用途、输入输出约束、错误条件和调用方需要遵守的前置条件。

不应添加注释的情况：
- 注释只是把代码逐字翻译成人话。
- 注释描述的是显而易见的控制流、变量赋值或函数调用。
- 注释与代码保持同步成本高，且不能提供额外信息。
- 用注释掩盖命名不清、结构混乱、函数过长或抽象不当的问题。
- 用注释解释死代码、隐藏 warning 或替代测试。

优先通过清晰命名、拆分函数和局部结构表达意图；只有当代码本身无法充分表达约束、背景或取舍时，再使用注释。

注释默认使用中文。Rust 标识符、trait 名、crate 名、协议名、错误码、命令行参数、第三方 API 名称等保持英文原文。需要同时照顾可读性和检索性时，使用“中文说明 + 英文术语”的形式。

推荐写法：
// 外部接口要求传入的是字节偏移，不能直接使用字符索引。
offset = to_byte_offset(text, char_index)
避免写法：
// 获取 offset
offset = to_byte_offset(text, char_index)

注释换行应按语义切分，而不是按字符长度机械换行。每一行注释都应表达一个相对完整的意思，相关说明可以分成多行，但不要把一个完整短语、条件或因果关系硬拆开。

推荐写法：
// 外部接口要求传入的是字节偏移。
// 这里必须先把字符索引转换为字节偏移，否则多字节字符会导致定位错误。
offset = to_byte_offset(text, char_index)
避免写法：
// 外部接口要求传入的是字节
// 偏移。这里必须先把字符索引转换为
// 字节偏移，否则多字节字符会导致定位错误。
offset = to_byte_offset(text, char_index)

多行注释应保持段落层次清晰：同一层语义连续说明，不要为了对齐、凑行宽或视觉整齐而拆分。若注释已经长到需要多段解释，优先考虑是否应该提取为文档注释、测试用例或设计说明

注释应尽量靠近被解释的代码。不要把原因、约束或风险写在远离相关代码的位置，避免后续修改时遗漏同步更新。

### 4. 模块组织规范

代码组织遵循 **高内聚、低耦合**。这是结构性规则，AI 在新增 / 重构代码时必须主动遵守。

**高内聚**：一段代码只服务一个消费方时，物理上跟那个消费方放在一起；不要让"被谁用"的关系散落在目录中。

```text
window_controls 只被 top_bar 用 →  shell/components/top_bar/window_controls.rs
                            而不是 shell/components/window_controls.rs
```

**低耦合**：被多个模块共用的原语单独抽出，让消费方各自依赖原语，而不是互相依赖。

```text
bar_frame / glyph / bar_region 被 top_bar 和 bottom_bar 共用
  →  shell/components/primitives/{bar_frame,glyph,bar_region}.rs
top_bar 不直接 import bottom_bar，反之亦然。
```

判断流程：

1. 新增组件或抽象前先看消费方数量。
2. 单一消费方 —— 内嵌或放进消费方目录。
3. 多个消费方 —— 抽到共享层（`primitives/`、`shared/`、`common/`，按上下文命名）。
4. 不要预建抽象层。先有 ≥ 2 个真实消费方再抽，避免"为未来准备"的死代码。

### 5. Warning 规范

不要用 `#[allow(...)]`、`#![allow(...)]` 或类似方式隐藏 warning。

warning 是协作信号，应当暴露出来。能当场解决就修掉；暂时不能解决时，保留 warning，并在回复、issue、TODO 或相关文档中说明原因、影响范围和后续处理方向。

确需保留例外时，必须先有明确的项目级约定，并在代码旁用中文说明为什么这个 lint 不适用于当前场景。

---

**如果以下情况发生，则这些准则有效：** 差异中不必要的更改较少，因过度复杂化而导致的重写较少，以及澄清问题发生在实现之前而不是错误之后。
