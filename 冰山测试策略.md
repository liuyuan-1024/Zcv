# 🧪 Zom Editor Engine 测试策略

> 本文档定义 `zom-engine` 的测试组织方式与测试边界。  
> 目标：保证编辑引擎作为独立 Rust crate 时，具备高正确性、高稳定性、低 Flaky 率，并能被后续 `zom ide` 安全引用。  
> 核心原则：**大部分测试验证纯编辑引擎逻辑，少量测试验证 GPUI 与编辑引擎的桥接。**

---

## 1. 测试策略总览：冰山模型

本项目采用“冰山模型”测试策略：

```text
水面之上：20%
GPUI testbed / UI 桥接测试 / 手动交互验证

水面之下：80%
纯 EditorEngine 测试 / 随机测试 / 差分测试 / 性能测试
```

### 水面之下：80% 纯 EditorEngine 测试

位置：

```text
src/tests/
tests/
benches/
```

目标：

- 验证编辑引擎核心逻辑正确性
- 验证复杂文本边界处理
- 验证事务、Undo、坐标、多光标等核心行为
- 验证大文件和超长行下的性能
- 尽量避免 UI 测试带来的不稳定性

特点：

- 不依赖 GPUI
- 不依赖窗口系统
- 不依赖真实渲染
- 运行速度快
- 适合 CI
- 不容易 flaky

### 水面之上：20% GPUI 集成测试

位置：

```text
examples/gpui_testbed.rs
```

目标：

- 验证 GPUI 事件能否正确进入编辑引擎
- 验证快捷键是否映射到正确 Command
- 验证 UI 状态是否能与 Engine 状态同步
- 验证后续 `zom ide` 使用 GPUI 时的基础桥接可行性

特点：

- 只测试 UI 桥接层
- 不重复测试编辑算法
- 可作为手动 / 半自动 testbed
- 不作为核心正确性的主要来源

---

## 2. 测试组织原则

本项目不采用“每个源码文件底部大量堆叠 `#[cfg(test)] mod tests`”的方式。

推荐采用集中式测试模块：

```text
zom-engine/
├── src/
│   ├── lib.rs
│   ├── buffer/
│   ├── storage/
│   ├── transaction/
│   ├── selection/
│   ├── history/
│   ├── coords/
│   └── tests/
│       ├── mod.rs
│       ├── helpers.rs
│       ├── edit_basic.rs
│       ├── transaction.rs
│       ├── undo_redo.rs
│       ├── coordinates.rs
│       ├── unicode.rs
│       ├── multi_cursor.rs
│       ├── tracked_range.rs
│       ├── search_replace.rs
│       ├── snapshot.rs
│       ├── changeset.rs
│       └── differential.rs
│
├── tests/
│   ├── public_api.rs
│   └── integration_smoke.rs
│
├── examples/
│   └── gpui_testbed.rs
│
└── benches/
    ├── edit.rs
    ├── coordinates.rs
    ├── large_file.rs
    └── random_edits.rs
```

---

## 3. `src/tests/`：核心引擎测试区

`src/tests/` 是主要测试区，用来测试 crate 内部行为和 `pub(crate)` 能力。

在 `src/lib.rs` 中声明：

```rust
#[cfg(test)]
mod tests;
```

在 `src/tests/mod.rs` 中集中组织测试模块：

```rust
mod helpers;

mod edit_basic;
mod transaction;
mod undo_redo;
mod coordinates;
mod unicode;
mod multi_cursor;
mod tracked_range;
mod search_replace;
mod snapshot;
mod changeset;
mod differential;
```

### 适合放在 `src/tests/` 的测试

- 基础编辑行为
- 事务系统
- Delta / ChangeSet / PositionMap
- Undo / Redo
- Snapshot
- 坐标转换
- UTF-16 Position
- Grapheme Boundary
- CRLF 边界
- Tab Width / Display Column
- 多光标
- Selection normalize / merge
- Anchor / TrackedRange
- 当前 Buffer 内搜索与替换
- 大文件与超长行边界
- 随机编辑序列
- 与 String 参考模型的差分测试

### 不建议在源码文件底部堆叠大量测试

不推荐：

```rust
// src/buffer.rs

#[cfg(test)]
mod tests {
    // 大量测试全部堆在这里
}
```

原因：

- 源码文件会变得很长
- 编辑器核心逻辑和测试逻辑混杂
- 后续测试数量增长后难以维护
- 不利于按能力域组织测试

### 什么时候可以在源码文件底部写测试？

少量非常局部的私有函数测试可以保留在源码文件底部，例如：

- 极小的纯函数
- 不值得暴露为 `pub(crate)` 的内部 helper
- 与模块私有状态高度绑定的边界测试

但这不作为主要测试组织方式。

---

## 4. `tests/`：公共 API 集成测试

`tests/` 目录用于模拟外部 crate 使用 `zom-engine`。

这些测试只能访问 public API：

```rust
use zom_engine::*;
```

适合测试：

- `zom-engine` 对外 API 是否稳定
- 外部项目能否正常创建 Buffer
- 外部项目能否提交 Transaction
- 外部项目能否读取 Snapshot
- 外部项目能否执行 Undo / Redo
- 外部项目能否消费 Delta
- 外部项目能否访问坐标转换接口

示例文件：

```text
tests/
├── public_api.rs
└── integration_smoke.rs
```

### `tests/` 不适合测试什么？

不适合测试：

- 私有函数
- `pub(crate)` 内部结构
- 存储结构内部细节
- 特定 Piece Tree 节点旋转逻辑
- 内部 cache 失效细节

这些应放在 `src/tests/` 或更局部的模块测试中。

---

## 5. `examples/gpui_testbed.rs`：GPUI 桥接测试台

`examples/gpui_testbed.rs` 用来验证 GPUI 与编辑引擎之间的桥接，而不是测试编辑算法本身。

运行方式：

```bash
cargo run --example gpui_testbed
```

### GPUI testbed 应该验证什么？

- GPUI 按键事件是否能进入 engine
- 快捷键是否映射到正确 Command
- `cmd-z` / `ctrl-z` 是否触发 Undo
- `cmd-shift-z` / `ctrl-y` 是否触发 Redo
- 普通字符输入是否提交 insert transaction
- 删除键是否提交 delete transaction
- 鼠标点击是否能更新光标
- 鼠标拖拽是否能更新 selection
- IME composition 事件是否能正确进入 engine
- viewport 是否能从 engine 获取可见文本
- UI selection 是否能与 engine selection 同步

### GPUI testbed 不应该测试什么？

不应该重复测试：

- Piece Tree 是否正确插入
- Undo 算法是否正确
- UTF-16 坐标是否正确
- 多光标重叠删除是否正确
- Grapheme 删除是否正确
- Search / Replace 算法是否正确

这些核心算法应放在纯引擎测试中。

---

## 6. `benches/`：性能基准测试

性能测试建议放在 `benches/` 中。

推荐使用：

```toml
[dev-dependencies]
criterion = "0.5"
```

建议覆盖：

```text
benches/
├── edit.rs
├── coordinates.rs
├── large_file.rs
└── random_edits.rs
```

### Benchmark 覆盖目标

- 单点插入
- 单点删除
- 范围替换
- 批量编辑
- ByteOffset <-> Line / Column
- UTF-16 Position 转换
- Snapshot 创建
- Undo / Redo
- Viewport slicing
- 超长行编辑
- 大文件随机编辑
- 多光标编辑
- 当前 Buffer 内搜索
- Replace All

---

## 7. 差分测试策略

差分测试是编辑引擎项目中非常重要的测试方式。

核心思路：

```text
高性能存储实现执行一组随机编辑
String 参考模型执行同一组随机编辑
最后比较两者文本是否完全一致
```

推荐模型：

```text
PieceTreeBuffer / RopeBuffer
        vs
StringBuffer
```

### 应覆盖的随机编辑

- 随机插入 ASCII
- 随机插入中文
- 随机插入 emoji
- 随机插入换行
- 随机删除单字符
- 随机删除 range
- 随机替换 range
- 随机 CRLF 文本
- 随机超长行
- 随机多光标编辑

### 差分测试目标

- 验证文本内容一致
- 验证行数一致
- 验证坐标转换一致
- 验证 undo 后一致
- 验证 redo 后一致
- 验证 snapshot 内容一致

---

## 8. Property-based Testing 策略

建议使用：

```toml
[dev-dependencies]
proptest = "1"
```

适合 property-based testing 的能力：

- 坐标互转
- EditList 归一化
- Selection merge
- Range 映射
- ChangeSet old -> new -> old
- Undo / Redo 往返
- Grapheme 边界
- CRLF 边界
- Display Column 转换

### 示例性质

```text
line_column_to_offset(offset_to_line_column(x)) == x

apply(edit)
undo()
文本应恢复原样

apply(edit)
undo()
redo()
文本应等于第一次 apply 后的文本

所有 normalized edits 应该有序且不重叠

所有 selection normalize 后应有序且不重叠
```

---

## 9. Fuzz Testing 策略

对于编辑引擎，fuzz testing 非常值得做。

建议后期加入：

```text
fuzz/
├── random_edit_sequence.rs
├── unicode_boundary.rs
├── changeset_mapping.rs
└── undo_redo_roundtrip.rs
```

可以重点 fuzz：

- 非法 UTF-8 边界
- Grapheme 边界
- CRLF 边界
- 随机 range
- 随机 transaction
- 随机多光标
- 随机 Undo / Redo
- 随机 ChangeSet 映射

---

## 10. 推荐测试文件划分

### `src/tests/edit_basic.rs`

测试：

- insert
- delete
- replace
- 空文本编辑
- 文首编辑
- 文末编辑
- 多行编辑

### `src/tests/transaction.rs`

测试：

- EditList 归一化
- 重叠 edit 检测
- 批量编辑
- Transaction metadata
- Transaction rollback
- Delta 生成

### `src/tests/changeset.rs`

测试：

- old position -> new position
- new position -> old position
- old range -> new range
- range 被删除后的失效策略
- anchor 映射
- selection 映射

### `src/tests/undo_redo.rs`

测试：

- 单步 undo
- 单步 redo
- 多步 undo
- 多步 redo
- selection restore
- transaction merge
- undo boundary

### `src/tests/coordinates.rs`

测试：

- ByteOffset <-> Line / Column
- UTF-16 Position
- CRLF 边界
- Tab Width
- Display Column

### `src/tests/unicode.rs`

测试：

- emoji
- ZWJ emoji
- 组合音标
- 中文
- 日文
- 韩文
- grapheme-safe delete

### `src/tests/multi_cursor.rs`

测试：

- 多光标插入
- 多光标删除
- 多光标替换
- 重叠 selection merge
- 矩形编辑基础行为

### `src/tests/tracked_range.rs`

测试：

- Anchor
- Mark
- TrackedRange
- stickiness
- range invalidation
- external metadata tracking

### `src/tests/search_replace.rs`

测试：

- 普通搜索
- 正则搜索
- whole word
- range search
- replace
- replace all as transaction

### `src/tests/differential.rs`

测试：

- StringStorage vs PieceTreeStorage
- 随机编辑序列
- 随机 Undo / Redo
- 随机坐标转换

---

## 11. CI 建议

核心 CI 建议运行：

```bash
cargo fmt --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test
```

后续加入：

```bash
cargo test --test public_api
cargo bench
```

GPUI testbed 不建议默认进入核心 CI，除非你有稳定的 GUI 测试环境。

---

## 12. 最终原则

- [ ] 核心编辑算法必须在纯测试中验证
- [ ] UI 测试只验证桥接，不验证算法
- [ ] 测试集中组织，避免散落在每个源码文件底部
- [ ] 优先测试 public / pub(crate) 行为，不依赖过多私有实现细节
- [ ] 高性能存储必须与简单 String 参考模型做差分测试
- [ ] Unicode、CRLF、Grapheme、Display Column 必须有专门测试
- [ ] Undo / Redo 必须测试文本和 selection 双恢复
- [ ] GPUI testbed 只承担“水面之上”的 20% 验证
