[package]
name = "zcv-buffer-diff"
version.workspace = true
edition.workspace = true
description = "行级 diff 数据模型（对齐 Zed buffer_diff 的类型归属层）"

[lib]
path = "src/buffer_diff.rs"

[dependencies]

[lints]
workspace = true
